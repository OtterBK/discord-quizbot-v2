# UI 개선안 (퀴즈만들기 + 사용자 UI 전반)

> 2026-08-05, `convertTagsValueToString`의 trailing comma 버그(태그 2개 이상 선택 시 "음악 퀴즈, 그림 퀴즈, "처럼 끝에 콤마가 남던 문제, `fix:` 커밋으로 이미 수정됨)를 계기로, 퀴즈만들기(`/퀴즈만들기`)와 그 주변 사용자 UI 전반의 가독성/일관성을 검토한 결과 기록.
>
> **성격이 다른 기존 문서와의 관계**: `DUPLICATE_UI_PATTERNS.md`는 코드 중복(같은 컴포넌트를 여러 곳에서 반복 정의) 관점, `BUGS_FOUND.md`는 로직 버그 관점이고, 이 문서는 **사용자가 실제로 보는 텍스트/버튼/흐름의 가독성·일관성·기대 불일치**만 다룬다. 코드 구조는 멀쩡한데 문구/라벨/색상 선택이 사용자에게 혼란을 주는 경우가 대상.
>
> 진행 상황(2026-08-06 기준): **0번(오타 3건), 2번(파괴적 동작 안전장치), 5번(주간 1위 데이터 오류) 수정 완료.** 남은 1/3/4번(용어·라벨 통일, 임베드 구조 개편, 흐름 불일치)은 여러 파일에 걸친 문구/스타일 정리라 아직 제안 단계. 우선순위는 맨 아래 "권장 진행 순서" 참고.

---

## 0. 즉시 수정 권장 (명백한 오타/버그, 각 1줄 수정) — ✅ 수정 완료 (2026-08-06)

이번 comma 버그와 같은 성격 — 판단이 필요 없는 명백한 실수라 바로 고쳐도 리스크가 없음.

1. **`quizbot/quiz_ui/components/custom_quiz_components.js:244`** — 문제 이미지 URL 필드 라벨의 대괄호가 안 닫혀 있고 불필요한 마침표가 붙어 있음:
   ```
   '문제와 함께 표시할 이미지입니다. [.WebP 형식은 사용 불가'
   ```
   힌트 이미지(`:285`)/정답 공개 이미지(`:350`) 필드는 정상 형식(`'... [WebP 형식은 사용 불가]'`)이라 셋을 통일해야 함. → `'... [WebP 형식은 사용 불가]'`로 통일.

2. **`quizbot/quiz_ui/components/custom_quiz_components.js:421`** — "주관식" 답변 유형 설명에 오타: `"플레이어는 메세지로 정답을 입력하는 방식입니다."` → "메시지"로 수정.

3. **`quizbot/quiz_ui/user-quiz-info.ui.js`** (삭제 완료 메시지) — `"퀴즈를 삭제했습니다.\r삭제한 퀴즈 ID: " + quiz_id` 에서 `\r`을 `\n`으로 수정하고, `quiz_id` 대신 퀴즈 제목을 보여주도록 변경(`user-quiz-list-ui.js:221`의 "수정 권한 없음" 메시지도 동일하게 quiz_id 노출 제거). 삭제 확인 프롬프트(`confirm_quiz_delete`)에도 대상 퀴즈 제목을 함께 표시하도록 추가.

---

## 1. 용어/라벨 일관성

같은 개념을 화면마다 다른 단어로 부르거나, 같은 단어를 다른 개념에 재사용하는 사례.

- **공개/비공개 토글 버튼** (`custom_quiz_components.js:29`, `quiz_toggle_public`) — 라벨이 항상 `'퀴즈 공개/비공개'` 고정이라, 지금 상태가 뭔지·눌렀을 때 어떻게 바뀌는지 버튼만 봐서는 알 수 없음. 결과는 편집 화면 제목의 `[공개]`/`[비공개🔒]` 배지로만 확인 가능하고, 조회(readonly) 화면에는 그 배지조차 없음.
- **"인증"(certified) 개념** — `user-quiz-info.ui.js:147`(인증여부 ⭕/❌), `quiz-info-ui.js:128`(인증 필터), `omakase_components.js:96`(인증된 퀴즈 필터) 세 군데서 반복 등장하지만, 어디에도 "인증"이 뭘 의미하는지(자동 조건? 관리자 심사?) 설명이 없음.
- **뒤로가기 버튼 라벨 3종** — 대부분 `'뒤로가기'`(4글자)인데 `base_components.js:141`(`control_btn_component`)만 `'뒤로'`(2글자). `custom_quiz_components.js:461`의 `btn_done`은 `customId('back')`으로 기능은 동일한 "뒤로가기"인데 라벨만 `'완료'`라 사용자 입장에선 완전히 다른 동작처럼 보임.
- **"퀴즈태그" vs "퀴즈 태그"** — `user-quiz-info.ui.js:149`는 붙여쓰기, `custom_quiz_components.js:73` select placeholder는 띄어쓰기. 같은 대상을 가리키는 용어의 표기가 다름.
- **"검색용 퀴즈 태그"라는 라벨의 오용** — `custom_quiz_components.js:69~80`의 `quiz_tags_select_menu`(placeholder: `'검색용 퀴즈 태그 선택하기 (여러 개 선택 가능)'`)는 실제로는 검색이 아니라 **내 퀴즈에 태그를 붙이는 편집 화면**에서 쓰임. "검색용"이라는 이름은 `quiz_search_tags_select_menu`(다른 컴포넌트)에 더 어울림.
- **"정답 이벤트 설정" 버튼 → "문제 정답 공개 시 설정" 모달** (`custom_quiz_components.js:388`, `:321`) — 버튼과 그 버튼이 여는 모달 제목이 "이벤트"/"공개"로 서로 다른 단어를 씀.
- **오마카세 장바구니 select 3종 모두 동일 placeholder** (`omakase_components.js:186~204`) — `'장바구니에 담긴 퀴즈 확인하기'`로 통일돼 있지만, 그 중 하나(`omakase_basket_select_menu`)는 선택 시 실제로 **항목을 장바구니에서 제거**함(제거 안내는 옵션 설명(dropdown 안쪽)에만 있어 눈에 잘 안 띔). "확인하기"라는 동사가 이 파괴적 동작을 전혀 암시하지 않음.
- **옵션 설명 유무의 비대칭** — `option_component`/`question_answer_type_select_menu`처럼 각 선택지마다 한 줄 설명이 달린 select가 있는 반면, 태그 선택 계열 select(`quiz_tags_select_menu`, 오마카세 태그 select 전부)는 라벨만 있고 설명이 전혀 없음. 사용자가 받는 안내 수준이 화면마다 들쭉날쭉.

---

## 2. 파괴적 동작에 대한 안전장치 불균형 — ✅ 수정 완료 (2026-08-06)

- **문제(질문) 단위 삭제 vs 퀴즈 전체 삭제** — 퀴즈 삭제는 확인 화면(취소/삭제 2버튼, `quiz_delete_confirm_comp`)을 거치는데, `question_edit_comp2`(`custom_quiz_components.js:399~411`)의 **"현재 문제 삭제"는 확인 절차 없이 원클릭으로 즉시 삭제**됨. 둘 다 되돌릴 수 없는 삭제인데 보호 수준이 다름. → `user-question-info-ui.js`에 확인 대기 상태(`pending_question_delete_confirm`)를 추가해, 같은 버튼을 한 번 더 눌러야 실제로 삭제되는 2클릭 방식으로 변경. 다른 버튼을 누르면 확인 대기가 자동 해제됨(오클릭 시 실수로 다음 클릭에 삭제되는 것 방지).
- **"삭제 + 제작자 영구밴" 버튼의 시각적 무게** (`custom_quiz_components.js:104~122`, `quiz_delete_confirm_admin_comp`) — "삭제만" 버튼과 "삭제+영구밴" 버튼이 나란히 **같은 `ButtonStyle.Danger`(빨강)**로 배치돼 있음. 콘텐츠 삭제와 계정 영구밴은 되돌리기 난이도가 전혀 다른 결정인데 색으로 구분이 안 됨. → Discord 버튼 스타일에 Danger보다 더 강한 단계가 없어 색 자체는 유지하되, "삭제+영구밴" 버튼을 별도 행으로 분리하고 라벨에 "⚠️ ... (해제 전까지 되돌릴 수 없음)" 경고 문구를 추가해 시각적/공간적으로 구분.
- **삭제 확인 프롬프트에 대상 정보 없음** — `user-quiz-info.ui.js:333`의 삭제 확인 문구("정말 해당 퀴즈를 삭제할까요?")와, 이후 다른 화면들의 오류 메시지(`user-quiz-list-ui.js:221` "당신은 해당 퀴즈를 수정할 권한이 없습니다. quiz_id: 123")가 **퀴즈 제목이 아니라 내부 quiz_id를 노출**함. 사용자가 실제로 기억하는 건 제목이지 숫자 ID가 아님. → 둘 다 quiz_id 대신 퀴즈 제목을 보여주도록 수정(0번 항목과 함께 처리).

---

## 3. 임베드 텍스트 구조/가독성

- **`user-quiz-info.ui.js`의 상세 정보 임베드(약 125~149줄)**가 볼드/백틱/수동 줄바꿈(`\n\n\n` vs `\n\n\n\n`)을 섞어 만든 하나의 긴 문자열임. discord.js의 embed `fields` 배열을 쓰면 제작자/생성일/수정일/플레이수/추천수/인증여부 같은 "통계성" 항목들을 표 형태로 깔끔하게 분리할 수 있음.
- **조사(단위) 오용** — 같은 파일에서 `` `플레이한 서버: ${played_count}개` ``(서버 수인데 "개"), `` `추천한 유저수: ${like_count}개` ``(사람 수인데 "개") — "곳"/"명" 등 자연스러운 단위로 바꾸는 게 좋음.
- **`quiz-info-ui.js`의 `getTagInfoText`(102~136줄)** — 어느 화면에서 호출되든 "📕 공식 퀴즈 설정"과 "📗 유저 퀴즈 설정" 두 블록을 **항상 같이** 보여줌. 오마카세 방에서 유저 퀴즈만 설정 중이어도 공식 퀴즈 설정 블록(공식 퀴즈 유형은 사실 `'음악 퀴즈'`로 하드코딩돼 있어 항상 같은 값)이 같이 떠서 화면이 불필요하게 길어짐.
- **모달 필드 라벨에 글자 수 제한이 전혀 언급되지 않음** — 퀴즈 제목(`minLength(4)`/`maxLength(40)`), 검색어(`maxLength(10)`) 등 여러 필드가 이런 제약을 갖고 있지만 라벨/placeholder 어디에도 안 적혀 있어, Discord의 기본 validation 실패 메시지로만 제약을 알게 됨.

---

## 4. 흐름/사용자 기대 불일치

- **메인 메뉴 "3️⃣ 퀴즈 만들기"를 눌러도 바로 만들기가 시작되지 않음** — `QuizToolGuideUI`(안내 화면)로 이동해서 "`/퀴즈만들기` 명령어를 입력해주세요"라는 문구만 보여줌. 사용자는 버튼을 눌렀는데 결국 다른 명령어를 또 입력해야 하는 흐름이라 첫 사용 경험에서 헷갈릴 수 있음.
- **"자유 텍스트로 예/아니오 표현" 패턴이 최소 3곳 반복** — 인증 필터 끄기(`quiz-info-ui.js:249`, `omakase_components.js:96`), 정답 대기시간 사용 여부(`custom_quiz_components.js:309`) 모두 `placeholder: '예시) 네'`처럼 "네"를 예시로 보여주지만, 실제 로직은 **아무 문자나 입력하면 true로 처리**함(빈 문자열만 false). 오타나 실수로 스페이스 하나만 입력해도 의도치 않게 켜질 수 있음 — select 메뉴나 버튼 토글로 바꾸면 오입력 자체가 불가능해짐.
- **"최근 장바구니 불러오기" 동작이 불명확** (`omakase_components.js:31~41`) — 현재 장바구니에 **추가(병합)**되는지 **덮어쓰기(교체)**되는지 라벨만으로는 알 수 없음.
- **`option_control_btn_component`의 "뒤로가기" 버튼이 빨간색(`ButtonStyle.Danger`)** (`base_components.js:172~175`, 서버 설정 화면) — 저장 안 하고 나가면 변경사항이 사라지는 걸 색으로 암시하는 것으로 보이나, 다른 화면의 "뒤로가기"는 전부 회색(`Secondary`)이라 단순 스타일 불일치처럼 보일 수 있음. "저장하지 않은 변경사항은 사라집니다" 같은 문구를 옆에 추가하면 의도가 명확해짐.

---

## 5. 데이터 정확성 문제 (텍스트가 사실과 다른 값을 보여줌) — ✅ 수정 완료 (2026-08-05)

- **`quizbot/quiz_ui/user-quiz-list-ui.js:238~270` (`sendQuizPlayedInfo`)** — 퀴즈 목록을 열 때마다 DM으로 보내는 안내문 중 `"이번 주에 가장 플레이된 퀴즈는 [...]이네요!"`가 `best_quiz`(전체 기간 1위)와 **완전히 동일한 `played_count`(전체 누적치) 필드**를 비교해서 뽑고 있었음.
  - 실제로는 DB에 주간 집계용 `played_count_of_week` 컬럼이 별도로 존재하고(`db_quiz.js`의 조회수 증가 쿼리에서 `played_count`와 함께 매번 갱신됨, `UserQuizInfo` 데이터에도 포함됨), **라이브 서버에서 매주 월요일 09시 cron으로 이 필드를 초기화**하고 있음 — 즉 주간 집계 인프라 자체는 이미 갖춰져 있었고, 이 화면 코드만 `played_count_of_week` 대신 실수로 `played_count`를 두 번 비교하고 있었던 단순 오타성 로직 버그.
  - `best_quiz_of_week` 비교 기준을 `quiz_info.data.played_count_of_week`로 수정 완료.

---

## 6. 낮은 우선순위 관찰 (당장 손 안 대도 되는 것들)

- 메인 메뉴 제목 `"🔔 퀴즈봇 [3]"`(`main-ui.js`)의 `[3]`이 사용자에게 아무 의미도 전달하지 못함(추정: 내부 버전/샤드 마커).
- `main_ui_component`의 "봇 공유" 버튼(`base_components.js:153~163`)이 실제로는 초대 링크가 아니라 koreanbots.dev(외부 투표 사이트)로 이동함 — 라벨만 보면 봇 초대 링크로 오해할 수 있음.
- `text_contents.json`의 `main_menu.description`이 실제로는 `main-ui.js`가 자체적으로 만드는 하드코딩된 설명(대기 중 로비 수 포함)에 밀려 렌더링에 전혀 안 쓰이는 죽은 문구로 보임 — 나중에 이 JSON만 고치고 "왜 안 바뀌지?" 헤맬 수 있음.

---

## 권장 진행 순서

1. ~~**0번(오타 3건)**~~ — 수정 완료(위 참고).
2. ~~**5번(주간 1위 데이터 오류)**~~ — 수정 완료(위 참고).
3. ~~**2번(파괴적 동작 안전장치)**~~ — 수정 완료(위 참고).
4. **1, 3, 4번**은 여러 파일에 걸친 문구/스타일 정리라 한 번에 몰아서 하기보다 화면 단위로 나눠서 점진적으로 처리하는 걸 권장. 특히 3번의 "embed fields로 구조화"는 `user-quiz-info.ui.js` 하나만 해도 꽤 큰 변경이라 별도 작업으로 분리하는 게 안전.
5. **6번**은 굳이 지금 안 고쳐도 됨 — 참고용 기록.

남은 건 1, 3, 4번(용어/라벨 통일, 임베드 구조 개편, 흐름 불일치 정리) — 화면 단위로 우선순위 골라주시면 그 항목부터 `fix:`/`refactor:` 커밋으로 나눠서 진행하겠습니다.
