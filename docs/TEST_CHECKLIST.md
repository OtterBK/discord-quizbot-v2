# 수동 테스트 체크리스트

> `develop-claude`에서 누적된 전체 구조 리팩터 + TS 마이그레이션 + 웹 연동 + 관리자 패널 등
> 신규 기능에 대한 라이브 테스트용 체크리스트 — `develop` 브랜치로 실서버에 설치해 전수 테스트할
> 때 사용. 하나씩 체크하면서 진행. 실제 대결(2개 이상 길드가 동시에 참여하는 멀티플레이 게임
> 진행)만 별도(섹션 K)로 분리 — 나머지는 혼자 테스트 가능.
>
> 문제를 발견하면 재현 조건 → 실제 동작 → 기대 동작 형태로 기록해두면 나중에 고칠 때 편함
> (`docs/archive/BUGS_FOUND.md`는 리팩터 시절 기록으로, 계속 쓰는 문서는 아님 — 새로 발견한 버그는
> `docs/ACTIVE_PLAN.md`나 새 이슈로 남길 것).

---

## A. 사전 준비

- [x] 봇이 정상적으로 기동되는지 (`Started Quizbot!` 로그 확인)
- [x] `/` 슬래시커맨드 목록에 명령어들이 정상 등록돼 보이는지 (`퀴즈`, `퀴즈만들기`, `quiz`, `답`, `퀴즈정리`, `챗`, `채팅전환`, `신고처리`, `quizmgr`)
- [x] 테스트용 부계정(멀티플레이/영구밴 테스트용) 최소 1개 준비돼 있는지

---

## B. 메인 메뉴 & 기본 네비게이션

- [x] `/퀴즈` 실행 → 메인 메뉴 정상 표시 (전체 서버 수/진행 중인 로컬·멀티 서버 수 필드 포함)
- [x] "1️⃣ 서버원과 게임하기" → `SelectQuizTypeUI`(개발자 퀴즈/유저 제작 퀴즈/랜덤 퀴즈 3버튼) 표시
- [x] "2️⃣ 다른 서버와 경쟁하기" → 멀티플레이 로비 목록 화면 진입 (섹션 J 참고)
- [x] "3️⃣ 퀴즈 만들기" → 안내 화면(`QuizToolGuideUI`) 표시, 뒤로가기 정상
- [x] "4️⃣ 서버 옵션 설정" → `ServerSettingUI` 진입 (섹션 F)
- [x] "5️⃣ 공지/패치노트" → `NotesSelectUI` 진입 (섹션 G)
- [x] 아무 화면에서나 "뒤로가기" 버튼 → 이전 화면으로 정상 복귀

---

## C. 개발자(공식) 퀴즈 플레이

`SelectQuizTypeUI` → "개발자 퀴즈" 경로.

- [x] 카테고리/폴더 트리 탐색 (하위 폴더 들어가기 → 뒤로가기로 상위 복귀)
- [x] 페이지네이션(다음/이전 페이지, 페이지 점프) 정상 동작
- [x] 퀴즈 시작 → `AlertQuizStartUI`("퀴즈 시작합니다") 표시 후 실제 진행
- [x] **문제 유형별로 최소 1개씩 플레이**해서 각각 정상 동작 확인:
  - [x] 노래 맞추기 (Song) — 오디오 재생, 정답 제출
  - [x] 이미지 맞추기 (Image / Image Long)
  - [x] 인트로 퀴즈 (Intro) — 오디오 재생 후 추가 정답 대기시간(10초) 있는지
  - [x] 텍스트 퀴즈 (Text / Text Long)
  - [x] OX 퀴즈 — 버튼으로 O/X 선택, 시간 종료 시 일괄 채점
- [x] 힌트 요청 (자동/투표/방장 방식 중 서버 설정값에 맞게 동작하는지)
- [x] 스킵 요청 (투표/방장)
- [x] `/답` 슬래시커맨드로 정답 제출 (단답형 문제에서)
- [x] 채팅으로 정답 직접 입력해서 채점되는지 (단답형 + `use_message_intent` 옵션 켜진 경우)
- [x] 정답자 없이 시간 종료(Timeover) → 정답 공개 화면 정상
- [x] 정답자 있음(CorrectAnswer) → 정답 공개 화면에 맞춘 사람 표시
- [x] 마지막 문제까지 진행 → Ending(순위 발표, 1~3등 메달) → 종료 화면
- [x] "그만하기"(force stop) 버튼으로 중간에 강제 종료 → 정상적으로 세션 정리되는지 (음성 채널에서 봇이 나가는지)
- [ ] 문제 진행 중 좋아요(👍) 버튼 클릭 → 정상 처리되는지 (같은 퀴즈 두 번 좋아요 시도 시 중복 방지되는지)

---

## D. 유저 제작 퀴즈 — 생성/편집/삭제

`SelectQuizTypeUI` → "유저 제작 퀴즈" 경로 + `/퀴즈만들기`.

- [ ] `/퀴즈만들기` (또는 메인 메뉴 3번 안내 화면의 버튼) → DM으로 퀴즈 목록 화면 전송
- [ ] 길드 채널에서 `/퀴즈만들기` 입력 시 "개인 메시지를 확인해주세요" 안내 + DM 유도 메시지/버튼 정상
- [ ] **새 퀴즈 생성**: 제목/설명 입력 모달 제출 → 목록에 추가되는지
- [ ] 문제 추가: 기본 정보(정답, 오디오/이미지/텍스트) 입력 → 저장
- [ ] 문제 추가 시 **50개 제한** 걸리는지 (대량 테스트가 부담스러우면 스킵 가능)
- [ ] 문제 수정: 기존 값이 모달에 미리 채워지는지, 수정 후 반영되는지
- [ ] 문제에 이미지 URL 넣고 수정 → 이미지가 바로 로드되는지 (재전송 방식으로 고쳐진 부분)
- [ ] 문제 삭제 → 목록에서 제거, 인접 문제로 자동 이동
- [ ] **(신규, B-3') 문제 복제** "현재 문제 복제" 버튼 → 복사본이 원본 바로 다음 위치에 삽입되고, 화면이 그 복사본으로 자동 이동하는지 (데이터가 원본과 동일하게 복사됐는지도 확인)
- [ ] **(신규, B-3')** 문제 50개 꽉 찬 상태에서 "현재 문제 복제" 버튼이 비활성화되고, 눌러도 제한 메시지가 뜨는지 (대량 테스트 부담스러우면 스킵 가능)
- [ ] **(신규, B-2) 문제/정답용 오디오 미리듣기** 버튼(2번째 행) → 캐시 있는 오디오면 에페메럴 메시지로 클립 첨부되고, 재생 시 실제 게임 재생 구간(audio_start~min(+최대재생시간, audio_end))과 일치하는지
- [ ] **(신규, B-2)** 캐시 없는 신규 오디오로 미리듣기 시도 → "다운로드 중" 에페메럴 안내 뜨고, 잠시 후 다시 누르면 정상 클립이 오는지
- [ ] **(신규, B-2)** 문제용/정답용 오디오 URL이 비어있는 상태에서 해당 미리듣기 버튼 클릭 → "설정돼있지 않습니다" 안내 뜨는지
- [ ] **(신규, B-2)** 에페메럴 메시지가 본인에게만 보이고 직접 지울 수 있는지 (다른 유저는 애초에 DM이라 무관)
- [ ] **(신규, B-2) 화면 레이아웃 변경 확인**: "이미지 재로드" 버튼이 2번째 행(미리듣기 버튼들과 같은 행)으로 이동했는지, "문제 유형 선택" 드롭다운이 맨 아래(5번째 행)로 이동했는지
- [ ] **(2026-08-08, B-2 피드백 5번 수정) 문제 복제** 실행 후 콘솔/로그에 `Interaction has already been acknowledged` 에러가 더 이상 안 남는지
- [ ] **(2026-08-08, B-2 피드백 4번 수정) 미리듣기 연타**: 캐시 없는 오디오로 미리듣기 버튼을 짧은 간격으로 여러 번 누름 → yt-dlp 다운로드가 한 번만 시작되는지(로그에 "already downloading" 확인, 다운로드 완료 후 다시 누르면 정상 클립이 오는지)
- [ ] **(2026-08-08, B-2 피드백 1번 수정) 퀴즈 태그 미선택 상태로 공개 전환 시도** → 안내 메시지가 실제로 에페메럴(본인만 보이고 직접 지울 수 있는 X 버튼 있음)로 오는지
- [ ] **(2026-08-08, B-2 피드백 1번 수정) 퀴즈 삭제 확인 다이얼로그**(일반/어드민 둘 다) → 확인 버튼 클릭 후 "삭제되었습니다" 메시지가 에페메럴로 오고, 확인 다이얼로그 메시지 자체도 정상적으로 사라지는지
- [ ] **(2026-08-08, B-2 피드백 1번 수정) 새 퀴즈 생성 실패 시나리오**(재현 어려우면 스킵 가능) → 실패 안내 메시지가 정상 전송되는지(내부적으로 reply→followUp 방식 변경)
- [ ] **(2026-08-08, B-2 피드백 1번 수정) 퀴즈함 담기**(랜덤/멀티플레이 로비에서 유저 퀴즈 퀴즈함 모드) → "담았습니다" 메시지가 실제로 에페메럴로 오는지(이전엔 길드 채널에 공개로 남던 버그)
- [ ] 퀴즈 정보 수정(제목/설명/썸네일) 모달
- [ ] 퀴즈 공개/비공개 전환 (태그 없이 공개 전환 시도 → 태그 선택 요구 메시지 뜨는지)
- [ ] 태그 선택 후 공개 전환 정상
- [ ] 만든 퀴즈 실제 플레이까지 정상 진행
- [ ] **퀴즈 삭제(일반 유저, 본인 퀴즈)**: 삭제 버튼 → 확인 화면에 **2버튼**(취소/삭제)만 뜨는지 (3버튼 아님)
- [ ] 삭제 확인 → 목록에서 사라지고 더 이상 플레이 불가한지
- [ ] 유저 퀴즈 목록 화면: 정렬(업데이트순/주간 인기순/전체 인기순/추천순/최신순/오래된순) 정상 동작
- [ ] **(2026-08-08, B-2 피드백 6번 수정)** 정렬 드롭다운 라벨이 "추천순"/"최신순"/"오래된순"으로(각각 기존 "전체 추천순"/"최신 퀴즈순"/"오래된 퀴즈순"에서) 바뀌었는지, 옵션 설명 문구는 그대로인지
- [ ] 태그 검색 필터 정상 동작
- [ ] 키워드 검색 + 페이지 점프(복합 모달) 정상 동작

---

## E. 랜덤 퀴즈 (로컬, 구 "오마카세 퀴즈" — 2026-08-08 사용자 노출 문구만 변경, 내부 코드는 omakase 유지)

- [ ] 랜덤 퀴즈방 진입 → 기본 설정(문제 수 등) 확인
- [ ] 유형 태그(개발자 퀴즈 유형)/장르 태그(유저 퀴즈 장르) 선택 → 태그 정보 텍스트 갱신
- [ ] 인증 필터 켜기/끄기 모달
- [ ] 퀴즈함 모드 전환 → 퀴즈 선택 화면(`UserQuizSelectUI`, 담기 모드)으로 이동
- [ ] 퀴즈함에 퀴즈 담기 (최대 25개 제한 확인은 선택)
- [ ] "퀴즈함에 퀴즈 더 담기" / "최근 퀴즈함으로 덮어쓰기" 버튼 동작
- [ ] 태그 미선택 + 퀴즈함 비어있는 상태로 시작 시도 → 시작 차단되는지(`checkTagSelected`)
- [ ] 랜덤 퀴즈 실제 시작 → 정상 진행

---

## F. 서버 설정

- [ ] `ServerSettingUI` 진입 → 옵션 목록(오디오 재생시간/힌트방식/스킵방식/유사정답허용/점수방식/오디오컷개선/메시지의도파악/최대순위표시/최대기회) 표시
- [ ] 옵션 선택 → 값 선택 메뉴로 전환
- [ ] 값 선택 시 "저장" 버튼이 비활성 → 활성으로 바뀌는지
- [ ] 저장 버튼 클릭 → 저장 성공 메시지 + 이후 재진입 시 값이 실제로 반영돼 있는지
- [ ] 값만 바꾸고 저장 안 한 채 뒤로가기 → 변경사항 폐기되는지 (의도된 동작)

---

## G. 공지사항 / 패치노트

- [ ] `NotesSelectUI` → 공지사항 목록 정상 표시
- [ ] 패치노트 목록 정상 표시
- [ ] 항목 클릭 → 본문(`NoteUI`) 정상 표시, 작성일(timestamp) 표시되는지

---

## H. 스코어보드

- [ ] 멀티플레이 관련 화면에서 스코어보드 진입
- [ ] 로딩 중 placeholder → 실제 데이터로 정상 갱신
- [ ] 현재 서버 랭킹(승/패/MMR) 표시
- [ ] 글로벌 TOP 10 표시
- [ ] 데이터 없음/조회 실패 시에도 에러 메시지로 자연스럽게 표시되는지 (깨지지 않는지)

---

## I. 노래 퀴즈 오디오 캐싱 (버그 수정 확인 — 중요)

- [ ] 이미 캐시된 노래로 퀴즈 플레이 → 즉시 재생(캐시 히트)
- [ ] **캐시에 없는 새 유튜브 링크**로 유저 퀴즈 문제 등록 후 처음 재생 → 다운로드 진행 후 정상 재생되는지
- [ ] 다운로드 실패 케이스(비공개 영상, 삭제된 영상, 길이 초과 등 하나 골라서) → 적절한 실패 메시지가 뜨는지 (다운로드 결과 오판정이 없는지 - 이번에 고친 stdout 파싱 버그 확인 포인트)
- [x] 오디오 재생 시작 지점(`audio_start`) 지정한 문제 → 정확한 지점부터 재생되는지 (SeekStream 경로) — 2026-08-08 사용자 실제 봇 검증 완료
- [x] **(2026-08-08, B-2 피드백 7번 수정)** `WebmSeeker.seek()`(Cues 테이블 기반 정확한 seek)를 처음 연결한 수정 — 사용자가 실제 봇으로 검증 완료(단, 소스만 고치고 `dist/`를 재빌드 안 하면 반영 안 되니 `npm run build` 필수 — PowerShell 실행정책 막히면 `npm.cmd run build`로 우회). 문제 있으면 `utility/SeekStream/SeekStream.js`의 `this.accurate_start_point` 관련 코드 되돌리는 것으로 즉시 롤백 가능.
- [x] **(2026-08-08, B-2 피드백 8번 수정)** `generatePreviewClipStream()`의 `.setStartTime()` → `.seekOutput()` 교체 — 사용자가 실제 봇으로 검증 완료(미리듣기 구간이 실제 재생 구간과 일치).

---

## J. 신고 처리 (버그 수정 확인)

- [ ] 채팅 메시지에 신고 버튼 → 신고 사유 입력 모달 제출 → "신고가 접수되었습니다" 응답
- [ ] **같은 메시지를 5분 이내에 다시 신고 시도** → "No Cache Content" 실패 없이 정상 접수되는지 (이번에 고친 캐시 갱신 버그의 핵심 재현 조건)
- [ ] 신고 접수 시 관리자 DM으로 알림 오는지
- [ ] `/신고처리` → 최근 신고 목록 조회
- [ ] 신고 항목의 "처벌" 버튼 → 유저 밴 처리 + 신고자에게 알림 발송
- [ ] **(신규, B-4)** "처벌" 버튼 클릭 시 **밴 당사자에게도** DM으로 정지 알림(신고된 원문 + 총 정지 횟수 + 만료일)이 가는지
- [ ] "반려" 버튼 → 신고 기록만 삭제, 밴 없음
- [ ] 처벌 후 후속 조치: "취소"(밴 취소) 버튼
- [ ] **(신규, B-4)** "취소" 버튼 클릭 시 **밴 당사자에게** "정지가 취소되었습니다" DM이 가는지 (총 횟수/만료일 대신 취소 문구로 대체되는지)
- [ ] 처벌 후 후속 조치: "추가처벌" 버튼 (밴 카운트 증가)
- [ ] **(신규, B-4)** "추가처벌" 버튼 클릭 시 **밴 당사자에게** 갱신된 정지 횟수/만료일로 DM이 다시 가는지
- [ ] 처벌 후 후속 조치: "길드정지" 버튼 (해당 길드 멀티플레이 밴)
- [ ] **(신규, B-4)** 자동 신고 처리(3분 주기, 욕설 필터 감지)로 밴됐을 때도 밴 당사자에게 동일하게 DM 알림이 가는지 (수동 테스트 어려우면 로그로 `notifyBannedUser` 호출 여부만 확인해도 됨)

---

## K. 멀티플레이 — 로비 단계까지 (실전 대결 제외, 혼자 테스트 가능)

- [ ] "다른 서버와 경쟁하기" → 로비 목록 화면 진입, 대기 중인 로비 없으면 빈 목록 정상 표시
- [ ] 로비 생성 → 랜덤 퀴즈 설정(태그/퀴즈함)과 동일한 UI로 설정 가능한지
- [ ] 로비 설정 변경 시 정상 반영(로컬 확인만, 다른 길드 동기화는 섹션 L에서)
- [ ] 최소 참가 서버 수(2개) 미달 상태에서 "시작" 시도 → 차단 메시지 뜨는지
- [ ] 로비 나가기 → 정상적으로 세션 정리되는지
- [ ] `/퀴즈정리` 명령어로 진행 중 세션 강제 정리 → 정상 동작

---

## L. 관리자 패널 (`/quizmgr`, 신규 기능 — 전체 플로우)

**준비**: 테스트용 부계정으로 유저 퀴즈 1개 미리 만들어두기(삭제+영구밴 테스트 대상용).

- [ ] 길드 채널에서 `/quizmgr` 입력(어드민 계정으로) → DM 유도 메시지 + "내부 도구 열기" 버튼 표시
- [ ] DM에서 `/quizmgr` 재입력 또는 버튼 클릭 → 관리자 패널 메인(3버튼: 밴 목록 관리/신고처리/퀴즈 관리) 표시
- [ ] **밴 목록 관리** 클릭 → 현재 비어있으면 "밴 목록이 비어있습니다" 옵션(비활성 선택메뉴)으로 표시
- [ ] **신고처리** 클릭 → `/신고처리`와 동일하게 신고 목록이 DM으로 오는지
- [ ] **퀴즈 관리** 클릭 → 본인 퀴즈뿐 아니라 **테스트용 부계정이 만든 퀴즈도** 함께 보이는지
- [ ] 퀴즈 관리 목록에서 부계정 퀴즈 선택 → 정상 열람/수정 가능한지 (어드민이라 권한 체크 통과)
- [ ] 그 퀴즈의 삭제 버튼 클릭 → 확인 화면에 **3버튼**(취소/삭제만/삭제+제작자 영구밴) 뜨는지 (본인 퀴즈 삭제 때와 다르게)
- [ ] "삭제 + 제작자 영구밴" 클릭 → 삭제 완료 + 영구밴 메시지 응답
- [ ] 관리자 패널 → 밴 목록 관리 재진입 → 방금 밴한 부계정 ID가 목록에 보이는지
- [ ] **부계정으로 `/퀴즈만들기` 시도** → "퀴즈 생성 권한이 영구적으로 제한되었습니다" 응답, 목록 화면 진입 안 되는지
- [ ] **부계정으로 멀티플레이 "다른 서버와 경쟁하기" → 로비 생성 또는 참가 시도** → 차단 메시지 뜨는지 (실전 대결 없이 이 단계에서만 확인)
- [ ] 밴 목록 관리에서 그 ID 선택 → 해제(unban)
- [ ] 해제 후 목록에서 사라지는지
- [ ] 해제 후 **부계정으로 다시 `/퀴즈만들기`** → 이제 정상 진입되는지 (원복 확인)
- [ ] 해제 후 **부계정으로 멀티플레이 로비 생성/참가** → 이제 정상 진입되는지 (원복 확인)
- [ ] (부계정 준비되면) **어드민이 아닌 계정**으로 `/quizmgr` 시도 → 아무 응답 없이 조용히 인터랙션 타임아웃되는지 (봇이 아예 반응 안 하는 게 정상)
- [ ] (가능하면) 비어드민 계정으로 길드에서 `btn_quizmgr` 같은 버튼이 애초에 보이지 않는지 확인 (버튼 자체가 DM 전용이라 일반적으로는 볼 일이 없음 — 참고용)

---

## M. 의도적 동작 변경 확인 (회귀 아님, 확인용)

- [ ] **어드민 본인**이 `/퀴즈만들기` 실행 → 이제 본인이 만든 퀴즈만 보이는지 (예전엔 전체 유저 퀴즈가 자동으로 같이 섞여 나왔음 — 이번에 명시적으로 "관리자 패널의 퀴즈 관리"로만 전체조회가 가능하도록 분리함)

---

## N. 실전 대결 (2개 이상 길드 필요 — 별도 일정)

- [ ] 길드 A에서 로비 생성, 길드 B에서 참가
- [ ] 로비 설정 변경 시 양쪽 다 동기화되는지 (EDIT_LOBBY 신호)
- [ ] 준비 완료 신호, 시작 시 양쪽 다 `AlertQuizStartUI`로 동시에 전환되는지
- [ ] 문제 동기화 (양쪽이 같은 문제를 같은 타이밍에 받는지)
- [ ] 정답 제출/힌트/스킵이 상대 길드에도 반영되는지 (투표 방식 등)
- [ ] 전체 채팅(`/챗`, `/채팅전환`) 동작
- [ ] 한쪽 길드가 중도 퇴장 → 방장 교체 정상 동작(방장이 나간 경우) 또는 정상 퇴장 처리
- [ ] 게임 종료 후 MVP 표시, 스코어보드(MMR) 반영
- [ ] 길드 강퇴(참가자 킥) 기능 동작
- [ ] 악성 길드 대응: 밴은 안 걸었지만 진행 중인 대결에서 강제 퇴장시키는 기능 — **아직 미구현**(향후 과제, 계획서 참고)

---

## O. 퀴즈 선택 웹 연동 — Phase 1 공식 퀴즈 (2026-08-08, 토큰 생명주기 2차 재설계까지 반영)

사전 준비: `npm run build`(백엔드 + `web-frontend`) 후 봇/마스터 재시작(루트 CLAUDE.md 경고 — 재빌드
안 하면 반영 안 됨), `config/system_setting.js`의 `WEB_BASE_URL`이 실제 접속 가능한 주소(로컬이면
`http://localhost:4321`)인지 확인.

> **2026-08-08 추가 변경 — 투트랙 진입점 분리**: `/퀴즈` 직후 곧장 `MainUI`를 띄우던 것을 "디스코드
> UI/웹 UI" 선택 화면(`SelectUIModeUI`)이 먼저 뜨도록 바꿈. 디스코드 선택 시 `MainUI`부터 완전히 기존
> 방식(공식 퀴즈도 `DevQuizSelectUI` 그대로, 웹 연동 전혀 관여 안 함), 웹 선택 시 `MainUI`/
> `SelectQuizTypeUI`를 건너뛰고 `WebHandoffUI('dev')`로 바로 진입. 이 변경으로 아래 체크리스트의
> "공식 퀴즈 클릭" 경로와 "뒤로가기/GC 만료 시 복귀 화면"이 바뀌었으니 **재검증 필요**(진입 경로만
> 바뀌었을 뿐 `WebHandoffUI` 내부 동작은 Phase 1 그대로라 재검증 우선순위는 낮음).

> 배경: 1차 실사용 테스트에서 "확정 후 문제 수 재변경 불가"/"확정 후 다른 퀴즈 선택 불가"/
> "invalid_or_expired_token 에러" 3건이 나왔음. 원인은 "선택 완료"가 서버 쪽에서 토큰을 즉시 파기하던
> 것 하나였음. 응급 수정(종료 화면으로 막기) 대신 근본 원인을 고쳐서, **토큰 파기 시점을 "퀴즈 실제
> 시작" 또는 "그 길드의 화면이 다른 이유로 사라질 때"로 옮기고**, `DevQuizInfoUI`가 직접 웹 신호에
> 반응하도록 만들어 확정 후에도 계속 재선택이 되게 바꿨음(`docs/plans/WEB_INTEGRATION_PLAN.md` 5-1 참고).
> 재설계 직후 순환 require 크래시가 하나 더 나왔고(퀴즈1 확정 → 퀴즈2로 재선택 시
> `Cannot read properties of undefined` 에러) 이것도 수정 완료.
>
> **2026-08-08 실사용 재검증 결과**: 아래 "기본 흐름"과 "확정 후 재선택 — 다른 퀴즈로 변경" 항목은
> 사용자가 실제 봇으로 정상 동작 확인함(`[x]` 표시). 나머지(문제 수만 재변경/뒤로가기 스택/세션 종료
> 시점/하이재킹/GC 만료/다크모드/탭 비활성화)는 **아직 미확인** — 다음 세션에서 시간 날 때 확인.

**투트랙 진입 (2026-08-08 신규, 미확인)**
- [ ] `/퀴즈` → "디스코드 UI/웹 UI 선택" 화면(`SelectUIModeUI`)이 뜨는지, 버튼 라벨/설명이 맞는지
- [ ] "디스코드 UI" 클릭 → 기존 `MainUI`(서버원과 게임하기/멀티플레이/퀴즈만들기/서버설정/공지 5개) 정상 표시, "서버원과 게임하기" → "공식 퀴즈" 클릭 시 웹 연동 전혀 없이 `DevQuizSelectUI`(폴더 트리) 그대로 뜨는지
- [ ] "웹 UI" 클릭 → `MainUI`/`SelectQuizTypeUI` 화면을 거치지 않고 곧바로 "🔒 ... 님이 웹에서 세팅 중입니다" 잠금 임베드가 뜨는지

**기본 흐름 (2026-08-08 실사용 확인 완료, 단 진입 경로는 위 투트랙 변경 이후 재확인 필요)**
- [x] (구 경로, 진입 지점만 `SelectUIModeUI`→"웹 UI"로 변경됨) 잠금 화면 + "🔗 웹에서 설정하기" 링크 버튼 표시
- [x] 링크 클릭 → 브라우저에서 토큰이 URL에 잠깐 보였다가 즉시 사라지는지(히스토리에 남지 않는지)
- [x] 웹에서 폴더 트리 탐색(하위 폴더 진입/브레드크럼으로 상위 이동) 정상 동작
- [x] 리프(실제 퀴즈) 선택 → 디스코드 채널 임베드가 실시간으로 "현재 선택: ..."로 갱신되는지
- [x] 문제 수 스테퍼(+/−, 직접 입력) 최대치가 해당 퀴즈의 실제 보유 문제 수로 제한되는지
- [x] "이 퀴즈로 선택 완료" 클릭 → 웹 버튼이 잠깐 "✓ 디스코드에 적용됨"으로 바뀌었다가 1.5초 뒤 원래대로 돌아오는지(계속 조작 가능한 상태 유지) + 채널이 `DevQuizInfoUI`(선택된 문제 수 반영)로 정상 전환되는지

**확정 후 재선택 (이번 재설계의 핵심)**
- [ ] "선택 완료" 이후에도 웹 화면의 트리/스테퍼가 계속 클릭 가능한 상태로 남아있는지(예전처럼 잠기지 않음)
- [ ] 확정한 채로 같은 퀴즈의 **문제 수만 바꾸고** 다시 "선택 완료" 클릭 → 디스코드의 `DevQuizInfoUI` 임베드가 새 문제 수로 즉시 갱신되는지 (미확인 — 다른 퀴즈로 바꾸는 경로만 확인됨)
- [x] 확정한 채로 **다른 퀴즈를 선택**하고 다시 "선택 완료" 클릭 → 디스코드 화면이 새 퀴즈 정보(제목/썸네일/설명)로 즉시 갱신되는지 — 화면이 깨지거나 예전 퀴즈 정보가 섞여서 남지 않는지 (순환 require 버그 수정 후 확인 완료)
- [ ] 여러 번 재선택을 반복한 뒤 디스코드에서 "뒤로가기"를 눌렀을 때 → `SelectUIModeUI`(디스코드 UI/웹 UI 선택 화면)로 바로 돌아가는지 (이전 웹 재선택 내역들이 뒤로가기 스택에 쌓여서 여러 번 눌러야 하는 문제가 없어야 함, **2026-08-08 진입점 변경으로 복귀 화면이 `SelectQuizTypeUI`→`SelectUIModeUI`로 바뀜**)

**세션 종료 시점 (신규 — 토큰 파기가 옮겨간 지점들)**
- [ ] 확정 후(퀴즈 시작 전) 디스코드에서 실제로 "시작" 버튼을 눌러 퀴즈를 시작 → 그 이후 웹 페이지에서 API 호출(예: 문제 수 변경) 시 "세션이 만료되었어요" 화면으로 전환되는지
- [ ] 확정 후(퀴즈 시작 전) **다른 계정**이 같은 서버에서 새로 `/퀴즈`를 눌러 완전히 새 화면을 띄움(하이재킹 아님 — `DevQuizInfoUI` 상태에서는 그냥 정상적으로 새 `MainUI`가 뜨는 게 맞는 동작인지도 같이 확인) → 그 뒤 **원래 사용자의 웹 세션**은 다음 API 호출에서 세션 만료로 전환되는지, 그리고 **새로 `/퀴즈` → 공식 퀴즈**를 눌렀을 때 "already_locked" 없이 정상적으로 새 웹 세션이 열리는지(고아 토큰 방지 확인 — 예전엔 여기서 막혔음)
- [ ] `/퀴즈정리` 명령어 실행 후에도 위와 동일하게 고아 토큰 없이 새 웹 세션이 정상적으로 열리는지

**세션 무효화 공통 처리**
- [ ] 세션이 어떤 이유로든(퀴즈 시작, 만료 GC, 다른 계정의 권한 가져오기) 무효화되면, 웹 화면 전체가
      항상 같은 문구("🔒 세션이 만료되었거나 잘못된 링크예요 ... [/퀴즈]를 다시 입력해주세요")로
      통일되게 보이는지 — `invalid_or_expired_token` 같은 원본 에러 문자열이 화면에 그대로 노출되지
      않는지
- [ ] 세션 무효화 이후 60초 대기 → 브라우저 개발자도구 네트워크 탭에서 `/api/session/heartbeat` 요청이
      더 이상 반복 발생하지 않는지(무효화 감지 시 하트비트 타이머가 멈추는지)

**기타**
- [ ] 다른 계정으로 **웹 세팅 잠금 화면이 떠 있는 동안**(`WebHandoffUI` 상태, 아직 확정 전) `/퀴즈` 실행 → 새 UI 대신 "🔒 ... 님이 웹에서 퀴즈를 세팅하는 중입니다" ephemeral 안내 + "🔓 권한 가져오기" 버튼 확인
- [ ] "권한 가져오기" 클릭 → 원래 세팅 중이던 계정의 웹 세션이 즉시 만료되는지(새로고침 시 "세션 만료" 안내), 새 계정 소유의 잠금 화면이 새로 생성되는지
- [ ] `WEB_SESSION_EXPIRE_SEC`을 짧게(예: 30) 임시 조정 후 웹에서 아무 조작 없이 대기 → 만료 시 채널이 이전 화면(`SelectUIModeUI`, **2026-08-08 진입점 변경으로 `SelectQuizTypeUI`에서 바뀜**)으로 자동 복귀하는지
- [ ] 웹 페이지 다크모드 토글(라이트/시스템/다크) 정상 동작, 시스템 설정 따라가는지
- [ ] **OS가 다크 모드여도 웹 페이지 첫 진입 시 기본값은 라이트 모드로 보이는지**(2026-08-09 피드백
      반영 — 기존 기본값 `'system'`이 다크 OS 사용자에게 다크로 보였음)
- [ ] "랜덤 퀴즈" 탭이 비활성화(disabled) 상태로 보이는지 확인(Phase 3 전까지 의도된 동작, **"유저 퀴즈"
      탭은 Phase 2 완료로 활성화됨 — 아래 P 섹션 참고**)

---

## P. 퀴즈 선택 웹 연동 — Phase 2 유저 퀴즈 (2026-08-08, 전부 미확인 — 다음 세션 최우선)

사전 준비는 O 섹션과 동일(`npm run build` 후 재시작 필수). 진입은 이제 `/퀴즈` → "웹 UI" 버튼(투트랙
진입점 분리, O 섹션 상단 참고) → 웹에서 "유저 퀴즈" 탭 클릭.

**목록/필터**
- [ ] "유저 퀴즈" 탭 클릭 시 실제 DB의 유저 퀴즈 목록이 카드 그리드로 표시되는지
- [ ] **카드에 썸네일과 함께 한줄 소개(simple_description)가 표시되는지**(2026-08-08 피드백 반영 —
      기존엔 카드에 제목만 있고 한줄 소개가 안 보였음)
- [ ] 검색창에 제목/한줄소개/제작자 이름 입력 시 클라이언트 사이드로 즉시 필터링되는지
- [ ] 태그 칩 여러 개 선택 시 AND 조건(선택한 태그를 전부 가진 퀴즈만)으로 필터링되는지, "전체" 클릭 시 초기화되는지
- [ ] "인증 퀴즈만" 토글 on 시 인증된 퀴즈만 보이는지
- [ ] **정렬(업데이트순/주간 인기순/전체 인기순/추천순/최신순/오래된순) 변경 시 목록 순서가 바뀌는지**
      (2026-08-09 피드백 반영 — 디스코드가 지원하는 정렬 목록과 정확히 일치해야 함, 없던 "이름순"이
      생기고 몇 개가 빠져 있었음)
- [ ] 카드에 마우스를 올리고 잠시(0.5초) 기다리면 플로팅 미리보기가 뜨는지(제목/제작자/한줄 소개/태그/
      좋아요), 마우스를 떼면 사라지는지(2026-08-09 피드백 반영)

**선택/확정**
- [ ] 카드 클릭 시 채널 임베드의 "현재 선택"이 실시간으로 갱신되는지
- [ ] **상세 패널에 디스코드 `UserQuizInfoUI`와 동일한 정보(썸네일 이미지, 한줄 소개, 태그, 전체 설명,
      좋아요/보유 문제/플레이한 서버 수, 만들어진 날짜/업데이트 날짜)가 전부 표시되는지**(2026-08-08
      피드백 반영 — 기존엔 좋아요/보유 문제 수만 있었음)
- [ ] **제작자/한줄 소개/상세 설명/태그가 각각 라벨과 함께 구분되어 표시되는지, 상세 설명이 태그보다
      위에 오는지**(2026-08-09 피드백 반영 — 기존엔 전부 같은 스타일 텍스트로 뭉쳐 있었고 순서도
      태그가 먼저였음, `QuizDetailCard.jsx`)
- [ ] **태그가 작은 알약이 아니라 블록 느낌으로 표시되는지**(2026-08-09 피드백 반영, `tag-block`)
- [ ] **상세 설명에 마크다운(굵게/기울임/목록/링크 등)이 적용된 텍스트를 넣으면 그대로 서식이
      적용돼 보이는지**(2026-08-09 피드백 반영, `react-markdown`) — 특히 `<script>`처럼 원시 HTML을
      넣었을 때 그대로 실행되지 않고 텍스트로만 보이는지(보안 확인)
- [ ] 상세 패널의 문제 수 스테퍼 최대치가 그 퀴즈의 실제 보유 문제 수(question_count)로 정확히 제한되는지
- [ ] "이 퀴즈로 선택 완료" 클릭 → 채널이 `UserQuizInfoUI`(선택된 문제 수 반영)로 정상 전환되는지
- [ ] 문제 수가 0개인 유저 퀴즈를 선택하면 확정 버튼 대신 "문제가 없어 시작할 수 없다"는 안내가 보이는지

**확정 후 재선택**
- [ ] 확정한 채로 **다른 유저 퀴즈**를 선택하고 다시 "선택 완료" → 채널이 새 퀴즈 정보로 즉시 갱신되는지
- [ ] 확정한 채로 같은 퀴즈의 **문제 수만 바꾸고** 다시 "선택 완료" → 채널의 문제 수가 갱신되는지
- [ ] "공식 퀴즈" 탭으로 전환해서 dev 모드로 선택/확정해도 정상 동작하는지(같은 세션에서 tab을 오가도
      한쪽 mode 처리가 깨지지 않는지 — `confirm`이 세션 고정 mode가 아니라 요청별 mode로 분기하도록
      바뀐 부분이라 특히 확인 필요)

**공통 인프라 재확인** (O 섹션에서 Phase 1로 검증됐지만 진입점이 바뀌어 재확인 필요)
- [ ] 뒤로가기 시 `SelectUIModeUI`로 복귀하는지
- [ ] GC 만료 시 `SelectUIModeUI`로 복귀하는지
- [ ] 하이재킹 방어("권한 가져오기")가 유저 퀴즈 탭에서 세팅 중일 때도 동일하게 동작하는지

---

## Q. 퀴즈 선택 웹 연동 — Phase 3 랜덤(오마카세) 퀴즈 (2026-08-08, 전부 미확인 — 다음 세션 최우선)

사전 준비는 O/P 섹션과 동일(`npm run build` 후 재시작 필수). 웹에서 "랜덤 퀴즈" 탭 클릭.

**공식 퀴즈 장르 + 모드 전환**
- [ ] 공식 퀴즈 장르 섹션이 강조 카드(골드 액센트 테두리/배경)로 눈에 띄게 표시되는지(2026-08-09
      가시성 개선 피드백 반영)
- [ ] 공식 퀴즈 장르 칩을 다중선택하면 제목 옆에 "N개 선택됨" 배지가 뜨고, 채널 임베드가 실시간으로
      갱신되는지
- [ ] 아무 장르도 선택 안 했을 때 우측 "설정 요약" 카드에 "🎯 공식 퀴즈 장르도 추가해보세요" 넛지가
      보이고, 클릭하면 공식 퀴즈 장르 섹션으로 부드럽게 스크롤되는지(하나라도 선택하면 넛지가 사라지는지)
- [ ] "장르로 뽑기"/"직접 담기" 버튼으로 모드 전환 시 하위 패널이 즉시 바뀌는지, 채널 임베드도 갱신되는지

**장르로 뽑기 모드**
- [ ] 퀴즈 유형 칩, 퀴즈 장르 칩 다중선택이 각각 정상 동작하는지
- [ ] "인증(추천 10개↑) 퀴즈만 출제" 토글이 정상 동작하는지
- [ ] 위 세 가지를 바꿀 때마다 우측 "설정 요약" 카드와 채널 임베드가 함께 갱신되는지

**직접 담기(퀴즈함) 모드**
- [ ] 검색/정렬(디스코드와 동일한 6개 옵션)/태그 필터가 정상 동작하는지(`/api/user-quizzes` 재사용 —
      유저 퀴즈 탭과 동일한 목록)
- [ ] **좌클릭하면 곧바로 담기는 게 아니라 상세 정보(썸네일/제작자/한줄 소개/상세 설명/태그/통계)가
      우측에 표시되는지**(예전엔 클릭=즉시 담기/빼기였음), 상세 패널의 "＋ 퀴즈함에 담기"/"✕ 퀴즈함에서
      빼기" 버튼으로 담기/빼기가 되는지
- [ ] **우클릭하면 상세보기를 열지 않고 곧바로 퀴즈함에 담기는지(담겨있으면 곧바로 빼기)**(2026-08-09
      피드백 반영 — 원래 승인된 목업 설계의 상호작용 복원), 브라우저 기본 우클릭 메뉴는 안 뜨는지
- [ ] 카드에 마우스를 올리고 잠시 기다리면 플로팅 미리보기가 뜨는지(2026-08-09 피드백 반영)
- [ ] 퀴즈함에 담긴 카드에는 체크 배지가 남아있는지(현재 미리보는 카드의 테두리 강조와 별개로 구분되는지)
- [ ] 🍱 FAB 버튼 클릭 시 드로워가 열리고 담긴 퀴즈 목록(제목)이 표시되는지
- [ ] **드로워의 퀴즈 항목을 클릭하면 우측에 그 퀴즈의 상세 정보가 표시되는지**, ✕로 개별 제거가
      되는지(제거 클릭이 상세 미리보기를 트리거하지 않는지)
- [ ] 퀴즈함에 25개 넘게 담아도 정상 동작하는지("디스코드 목록 UI는 25개까지만 지원하지만 웹에서는
      자유롭게 담을 수 있다"는 안내 문구가 25개 초과 시 보이는지)

**설정 요약 / 확정**
- [ ] 설정 요약 카드 제목이 "🍴 랜덤 퀴즈 설정 요약"으로 보이는지(2026-08-09 피드백 반영 — 기존
      "오마카세 설정 요약"에서 변경)
- [ ] 설정 요약 카드가 현재 조합(공식 장르/유저 모드/유형·장르 또는 퀴즈함 개수/인증 필터)을 정확히
      텍스트로 보여주는지
- [ ] 아무것도 선택하지 않은 초기 상태에서는 확정 버튼이 비활성화되고 안내 문구가 보이는지
- [ ] 목록/그리드를 한참 아래로 스크롤해도 우측 설정 요약/상세 패널이 뷰포트에 계속 보이는지(sticky)
- [ ] **직접 담기 모드에서 퀴즈를 선택해 설정 요약 카드 아래 상세 미리보기 카드가 함께 뜬 상태에서,
      상세 설명이 긴 퀴즈를 봐도 내용이 화면 밖으로 잘리지 않고 그 영역 안에서 스크롤되는지**
      (2026-08-09 피드백 반영 — 두 카드가 같이 쌓이면 sticky 영역이 뷰포트보다 커져 잘려 보였음)
- [ ] 문제 수 스테퍼(+/−, 직접 입력)가 1~100 범위로 정상 동작하는지
- [ ] "이 설정으로 선택 완료" 클릭 → 채널이 `OmakaseQuizRoomUI`로 정상 전환되는지(선택한 태그/퀴즈함/
      문제 수가 그대로 반영됐는지 임베드 텍스트로 확인)

**확정 후 재선택**
- [ ] 확정한 채로 태그나 퀴즈함을 다시 바꾸고 "선택 완료" → 채널이 즉시 새 설정으로 갱신되는지(같은
      화면이 유지되고 뒤로가기 스택이 쌓이지 않는지)
- [ ] 확정 후 디스코드에서 "시작" 버튼을 눌렀을 때, 아무 태그/퀴즈함도 선택 안 한 상태면 여전히
      "유형 및 장르를 1개라도 선택해주세요" 안내가 뜨는지(웹에서 확정해도 이 검증은 그대로 살아있어야 함)

## R. 퀴즈 선택 웹 연동 — Phase 4 멀티플레이 퀴즈 (2026-08-10, 실사용 피드백 10건 반영, 재검증 필요)

사전 준비는 O/P/Q 섹션과 동일(`npm run build` 후 재시작 필수). 웹에서 "멀티플레이 퀴즈" 탭 클릭.
**음성채널 체크가 실제로 걸리므로, 테스트 전 반드시 디스코드 음성채널에 먼저 들어가 있을 것.**

**대기실 목록**
- [ ] "멀티플레이 퀴즈" 탭 클릭 시 대기 중인 로비 목록이 `/멀티퀴즈`의 디스코드 목록과 동일한 내용
      (상태/참가 인원/호스트/평균 MMR)으로 보이는지
- [ ] 로비가 하나도 없을 때 안내 문구가 보이는지, "🔄" 아이콘 새로고침 버튼(정사각형 아이콘 버튼
      스타일, 2026-08-10 재작업)이 정상 동작하는지, 로딩 중엔 "⏳"로 바뀌는지
- [ ] "🌐 새 로비 만들기" 버튼(내용 너비만 차지하는 강조 버튼으로 재작업됨 - 예전엔 `.cta-primary`가
      `width:100%`라 toolbar 레이아웃이 깨져있었음)이 새로고침 버튼과 나란히 자연스럽게 배치되는지
- [ ] 게임 중인 로비는 "참가하기" 버튼이 비활성화된 채로 "🎮 게임 중"으로 표시되는지

**새 로비 만들기**
- [ ] "새 로비 만들기" 클릭 → 방 제목(1~20자, 앱 디자인에 맞는 입력칸 스타일)+랜덤 퀴즈와 동일한
      셰이프의 설정 화면(공식 장르 칩/유저 퀴즈 장르모드·바구니모드/퀴즈함 브라우징)이 보이는지
- [ ] **"직접 골라 담기" 모드에서 카드를 하나라도 담으면 우측 하단에 "🍱 퀴즈함" FAB 버튼이 나타나고,
      클릭하면 드로워가 열려 담긴 목록/개수가 보이는지, 항목 클릭 시 상세 미리보기로 연결되는지,
      ✕로 개별 제거가 되는지**(2026-08-10 수정 - 최초 구현 시 이 FAB/드로워 자체가 빠져있어서
      "직접 골라 담기" 모드에서 지금까지 담은 걸 한눈에 확인할 방법이 없었음)
- [ ] **문제 수 스테퍼가 20~60 범위로 동작하는지**(랜덤 퀴즈의 1~100과 다름 - 2026-08-10 수정 전에는
      하한이 없어서 20 미만도 입력 가능했는데, 확정하면 백엔드가 20으로 올려버려 사용자가 설정한
      값과 실제 반영된 값이 달라지는 문제가 있었음). 20 밑으로는 −버튼이 비활성화되고 직접 입력해도
      20 밑으로 안 내려가는지
- [ ] 방 제목 미입력 또는 아무 장르/퀴즈함도 선택 안 하면 "이 설정으로 로비 만들기" 버튼이 비활성화되는지
- [ ] 음성채널에 들어간 상태로 "이 설정으로 로비 만들기" 클릭 → "✓ 디스코드에 반영됨" 토스트 표시,
      같은 화면에 계속 머무름(완료 화면으로 넘어가지 않음) → 디스코드 채널이
      `MultiplayerQuizLobbyUI`(호스트)로 전환되고 설정(제목/태그/퀴즈함/문제 수)이 그대로 반영됐는지
- [ ] **음성채널 미접속 상태로 "이 설정으로 로비 만들기" 클릭** → 설정 화면에 빨간 오류 배너로 실패
      사유("음성 채널에 참가한 상태로 진행해주세요.")가 표시되는지(2026-08-10 수정 - 예전엔 아무 표시
      없이 조용히 실패했음), 디스코드 채널에도 같은 사유가 잠금 화면에 얹혀 보이는지
- [ ] **위 실패 후 음성채널에 들어간 다음 다시 시도** → 이번엔 정상적으로 로비가 생성되는지(2026-08-10
      수정 전에는 최초 실패 시점에 `WebHandoffUI`가 사라져서 재시도해도 완전히 먹통이었음 - 회귀
      확인용 핵심 케이스)
- [ ] **로비 생성 후에도 같은 화면에서 계속 태그/퀴즈함/문제 수/방 제목을 바꾸고 재확정** → "🌐 변경사항
      반영" 버튼 클릭마다 디스코드 로비 화면이 즉시 갱신되고, 참가 중인 다른 길드에게도 "로비 정보가
      변경되었습니다" 알림이 가는지(`onReceivedWebSessionSignal`) — 이 상태에선 "대기실 목록으로"
      버튼이 안 보이고 안내 문구만 보이는지(중복 로비 생성 방지)

**참가**
- [ ] 대기 중인 로비의 "참가하기" 클릭 → 완료 안내 화면 표시 → 디스코드 채널이
      `MultiplayerQuizLobbyUI`(참가자, readonly)로 전환되는지
- [ ] **완료 안내 화면에 "대기실 목록으로" 버튼이 없는지**(2026-08-10 수정 - 참가 후엔 디스코드
      UI로만 조작하도록 의도적으로 뺌), "지금부터는 디스코드 채널에서..." 안내 문구만 보이는지
- [ ] 음성채널 미접속 상태로 참가 시도 → 목록 화면에 빨간 오류 배너로 실패 사유가 뜨는지(완료 화면으로
      넘어가지 않는지)
- [ ] 밴된 서버/유저로 참가·생성 시도 → 즉시(폴링 없이) 오류가 뜨는지(마스터에서 선제 차단, 디스코드
      쪽과 동일하게 차단되는지)

**웹 세션 장시간 유지(코드 트레이스로만 확인됨 - 실제 장시간 테스트 필요)**
- [ ] 로비를 만든 뒤 웹 페이지를 열어둔 채로 15분 이상 방치 → 하트비트가 계속 성공하는지(개발자
      도구 네트워크 탭에서 `/api/session/heartbeat` 60초 간격 200 확인), 그 시점에 웹에서 설정을
      바꿔도 여전히 반영되는지(세션 만료 화면으로 안 넘어가는지)
- [ ] (참고용, Phase 4와 무관한 기존 동작) 로비를 만들기 **전** 잠금 화면 상태로 15분 이상 방치 →
      `UIHolder` 자체가 idle 정리돼서 세션이 끊기는지(예상된 동작, 다른 탭도 공통) - 문제로
      보고할 필요 없음, 참고로만 확인.

## S. 퀴즈 선택 웹 연동 — 전역 강조색 변경 (2026-08-10, gold → blue, 전체 탭 공통)

`styles.css`의 `--gold`/`-ink`/`-bg`를 `--primary`/`-ink`/`-bg`로 리네임하며 색상값도 파란색 계열로
교체 - 멀티플레이 탭만이 아니라 웹 UI 전체(Dev/User/Omakase/Multiplayer 4개 탭)에 영향을 준다.
라이트/다크 모드 둘 다 확인할 것(우측 상단 테마 토글).

- [ ] "공식 퀴즈"/"유저 퀴즈"/"랜덤 퀴즈"/"멀티플레이 퀴즈" 4개 탭 전부에서 주요 확정 버튼
      (cta-primary류: "선택 완료", "이 설정으로 로비 만들기" 등)이 파란색 배경 + 흰 글씨로 보이는지,
      가독성에 문제없는지(특히 다크 모드에서 대비가 너무 약하지 않은지)
- [ ] 탭 밑줄(활성 탭 표시)/태그 선택 강조(`.tag-chip.active`는 violet 유지 - 이건 안 바뀜, 헷갈리지
      말 것)/랜덤 퀴즈의 "공식 퀴즈 장르" 강조 카드(`.section-block.accent`)/문제 수 스테퍼 hover가
      전부 파란 계열로 일관되게 보이는지
- [ ] 브랜드 아이콘(좌상단 🎯 배지)의 그라디언트도 파란 계열로 바뀌었는지
- [ ] 텍스트 드래그 선택 시 하이라이트 색도 파란 계열인지(`::selection`)

**2클러스터 이상 환경(선택, `totalClusters`를 임시로 2 이상 지정해야 확인 가능)**
- [ ] 로비를 만든 길드와 다른 클러스터가 담당하는 길드에서 웹으로 참가해도 정상 동작하는지(IPC
      브로드캐스트+로컬필터링 경로 검증)

## T. 퀴즈 만들기 웹 연동 — Phase 1 세션 스코프 일반화 + 진입점 스켈레톤 (2026-08-11, 핵심 흐름 확인됨)

`docs/plans/WEB_QUIZ_CREATION_PLAN.md` Phase 1. 이 섹션 자체는 DM 투트랙 진입 화면과 잠금 화면만 확인(퀴즈
메타데이터 CRUD는 Phase 3 완료 - U 섹션 참고, 문제 CRUD는 Phase 4 완료 - V 섹션 참고). 사전 준비는 O
섹션과 동일(`npm run build` 백엔드+`web-frontend` 후 재시작 필요).

> **2026-08-11 후속 변경**: 편집 링크가 `.html` 확장자로 뜨는 게 촌스럽다는 피드백으로 URL을
> `/editor.html?token=...`에서 `/editor?token=...`로 변경(`web_express_app.ts`에 `/editor`/`/editor/*`
> 라우트 추가). 아래 "링크 클릭" 항목은 이제 확장자 없는 `/editor` URL 기준.

**투트랙 진입**
- [x] DM에서 `/퀴즈만들기` 입력 → "🛠 퀴즈 제작 방식 선택"(`QuizEditSelectUIModeUI`) 화면이 뜨는지
      (2026-08-11 사용자 확인)
- [x] "디스코드 UI" 클릭 → 기존 `UserQuizListUI`(내 퀴즈 목록)가 예전과 100% 동일하게 뜨는지(회귀
      확인 — 리스트/생성/편집/삭제 전부 기존과 같아야 함) (2026-08-11 사용자 확인)
- [x] "웹 UI" 클릭 → "🛠 퀴즈 웹 편집기" 잠금 임베드 + "🔗 웹에서 편집하기" 링크 버튼이 뜨는지
      (2026-08-11 사용자 확인)
- [x] 링크 클릭 → 브라우저에서 편집기(`/editor`)가 열리고 세션 인증되는지 (2026-08-11 사용자 확인 —
      "editor.html 에 접속됐음". "곧 편집 화면이 추가될 예정이에요" 플레이스홀더/`session.owner_name`
      표시까지는 별도 확인 안 됨)
- [ ] DM에서 `/퀴즈만들기`를 다시 입력 → 기존 세션/화면이 조용히 정리되고 새 투트랙 화면이 뜨는지(DM은
      1:1이라 하이재킹 방어 자체가 없음 — 그냥 무조건 교체되는지만 확인)
- [ ] GC 만료(`WEB_SESSION_EXPIRE_SEC`을 임시로 짧게 설정) 후 잠금 화면이 만료 안내로 바뀌는지
- [ ] 웹 링크로 편집기를 열어둔 채로 DM 쪽 잠금 화면을 벗어나도(예: 다른 명령어 입력으로 홀더 소멸)
      마스터의 owner 세션이 같이 정리되는지(고아 토큰 방지 — `release_owner_session` 확인, 직접적인
      화면 확인은 어려우니 이후 세션에서 같은 owner_id로 재진입 시 문제없이 새 세션이 열리는지로 간접
      확인)

## U. 퀴즈 만들기 웹 연동 — Phase 3 퀴즈 메타데이터 REST CRUD (2026-08-11 구현, 2026-08-12 실사용 검증 완료)

`docs/plans/WEB_QUIZ_CREATION_PLAN.md` Phase 3. 문제(question) 자체의 추가/수정/삭제는 아직 안 됨(Phase 4) —
이 섹션은 퀴즈 목록/생성/메타데이터/태그/공개토글/삭제만 확인. `/editor` 링크로 들어가면 이제 T 섹션의
플레이스홀더 대신 실제 목록 화면이 떠야 한다.

**목록 + 생성**
- [x] `/editor` 진입 시 보유한 퀴즈 목록(공개+비공개 전부)이 카드로 뜨는지, 카드에 🔒비공개/🌐공개
      배지가 정확한지
- [x] "+ 새 퀴즈 만들기" → 제목(4~40자)/한줄 소개/상세 설명/썸네일 입력 폼이 뜨는지, 제목 4자 미만
      입력 시 서버가 400을 반환해 에러 문구가 표시되는지
- [x] 생성 성공 시 방금 만든 퀴즈의 상세 화면(`/quiz/:quizId`)으로 자동 이동하는지, 새 퀴즈가
      기본적으로 비공개 상태인지

**상세 - 메타데이터/태그/공개토글**
- [x] 카드 클릭 → 상세 화면 진입, 기존 값이 폼에 미리 채워져 있는지
- [x] 제목/소개/설명/썸네일 수정 후 저장 → 목록으로 돌아갔다 다시 들어가도 값이 유지되는지
- [x] 태그 칩 클릭 → 즉시 저장되는지(별도 저장 버튼 없음, 디스코드 `editTagsInfo`와 동일), 새로고침 후
      선택 상태가 유지되는지
- [x] 태그가 하나도 없는 상태에서 "공개로 전환" 클릭 → "태그를 1개 이상 선택해주세요" 에러가 뜨고
      비공개로 유지되는지
- [x] 태그를 1개 이상 선택한 뒤 "공개로 전환" → 공개로 바뀌는지, 이후 "비공개로 전환"은 태그 체크 없이
      바로 되는지
- [x] 디스코드 쪽(`UserQuizListUI`→`UserQuizInfoUI`)에서 같은 퀴즈를 열어 웹에서 바꾼 내용(제목/태그/
      공개여부)이 반영돼 보이는지(실시간 반영은 아님 - 디스코드 쪽에서 새로 열었을 때 최신값인지만 확인)

**삭제**
- [x] "🗑 퀴즈 삭제" 버튼 → 첫 클릭은 "정말 삭제하려면 한 번 더 눌러주세요"로 바뀌고, 다른 조작을
      하면 원래 라벨로 돌아가는지(구현 안 돼있으면 버그로 기록 - 현재 프론트는 리셋 로직이 없을 수
      있음, 착수 시 코드 재확인)
- [x] 두 번째 클릭 → 목록 화면으로 돌아가고, 그 퀴즈가 목록에서 사라지는지(소프트 삭제라 DB에는 남되
      `is_use=false` - 디스코드 쪽에서도 더 이상 안 보이는지)

**문제 목록 (Phase 4에서 실제 CRUD 패널로 교체됨 — 상세 체크리스트는 V 섹션 참고)**
- [x] 상세 화면에 "문제 목록 (N/50개)"이 뜨고, 각 행 클릭 시 문제 수정 화면으로 이동하는지
- [x] 문제 URL이 잘못된 경우(디스코드에서 미리 잘못 입력해둔 퀴즈로 테스트) 문제 행에 ⚠️ 표시가 뜨는지

**UI 피드백 3건 (2026-08-11 추가, 2026-08-12 검증 완료)**
- [x] 상세 화면 우측에 "👀 다른 사람들에게 이렇게 보여요" 카드가 뜨는지, 썸네일 URL/제목/한줄
      소개/상세 설명/태그를 입력·수정하는 즉시(저장 버튼 누르기 전에도) 카드에 반영되는지
- [x] breadcrumb("📑 내 퀴즈 / 퀴즈제목") 우측에 `←` 아이콘 버튼이 있는지, 클릭 시 목록으로 돌아가는지
- [x] `/editor` 우측 상단에 라이트/시스템/다크 테마 토글이 뜨는지(기존엔 아예 없어서 다크모드
      강제됐음), 라이트가 기본값인지, 다크로 전환 시 실제로 색이 바뀌는지
- [x] 퀴즈 선택 웹 UI(`/`, 기존 index.html)도 같은 토글이 여전히 정상 동작하는지(`ThemeToggle`을
      공유 컴포넌트로 분리한 리팩터라 회귀 확인 필요)

## V. 퀴즈 만들기 웹 연동 — Phase 4 문제(Question) CRUD (2026-08-11 구현, 2026-08-12 실사용 검증 완료)

`docs/plans/WEB_QUIZ_CREATION_PLAN.md` Phase 4. 사전 준비는 O 섹션과 동일 + 비공개 퀴즈 1개를 미리 만들어둘
것(문제 최대 50개 테스트를 위해 문제가 몇 개 있는 퀴즈면 더 좋음). U 섹션 완료 후 이어서 진행.

**문제 추가**
- [x] 퀴즈 상세에서 "+ 새 문제 추가" → `/quiz/:quizId/questions/new`로 이동, 3개 탭(①기본 정보/②힌트
      설정/③정답 공개 설정)이 전부 뜨는지
- [x] 정답 유형을 주관식/OX/객관식으로 전환하면 정답 입력 UI가 바뀌고(자유 텍스트 → O/X 버튼 →
      1~5 버튼), 유형 전환 시 이전 입력값이 초기화되는지
- [x] 정답을 비운 채 저장 시도 → 주관식은 브라우저 필수값 검증으로 막힘. **OX/객관식은 원래 안 막히고
      서버 400(`invalid_answers`)이 raw 문자열로 그대로 노출되던 버그 발견 → 같은 세션에서 수정**
      (`QuestionEditPage.jsx`의 `handleSave`에 클라이언트 사전 체크 추가, "정답을 선택하거나
      입력해주세요." 한글 안내로 교체). 재검증 완료.
- [x] 오디오 URL + 재생 시작/끝(초) 입력 후 유튜브 인라인 임베드 플레이어가 지정 시작 지점부터
      재생되는지(2차 피드백으로 새 탭 링크 방식에서 임베드로 교체됨)
- [x] 이미지 URL 입력 시 바로 아래에 `<img>` 미리보기가 뜨는지(디스코드처럼 재전송 없이 즉시 반영)
- [x] 디스코드 CDN(`cdn.discordapp.com`) 이미지 URL 입력 시 타이핑 즉시 "일정 시간이 지나면 사라져요"
      경고가 뜨는지(저장 전에도)
- [x] 저장 → 퀴즈 상세로 돌아가고 방금 만든 문제가 목록 맨 끝에 보이는지

**문제 수정**
- [x] 문제 목록의 행 클릭 → 기존 값이 전부 폼에 채워진 채로 수정 화면이 뜨는지(정답 유형/오디오
      시작·끝초/체크박스 포함)
- [x] 한 필드만 바꾸고 저장 → 다른 필드들이 그대로 유지되는지(디스코드에서 열어서 확인해도 됨)
- [x] 저장된 문제의 오디오/이미지 URL이 잘못된 형식이면 해당 필드 아래에 "저장된 URL이 사용 불가능한
      형식이에요" 배지가 뜨는지(서버 스냅샷 기준 — 필드를 고쳐도 저장 전까지는 배지가 안 바뀌는지 확인)

**문제 삭제/복제**
- [x] 목록 행의 🗑 버튼 → 첫 클릭은 ⚠️ 아이콘으로 바뀌고, 두 번째 클릭에 실제 삭제되어 목록에서
      사라지는지(디스코드에서도 사라지는지)
- [x] 목록 행의 ⧉(복제) 버튼 → 같은 내용의 새 문제가 생성되고 목록 맨 끝에 추가되는지
- [x] 문제를 50개까지 채운 퀴즈에서 "+ 새 문제 추가"/각 행 "복제" 버튼이 비활성화되는지, 개발자
      도구로 강제 POST 요청을 보내면 서버가 400(`max_questions_reached`)을 반환하는지

**동기화**
- [x] 웹에서 추가/수정/삭제/복제한 문제가 디스코드 `UserQuestionInfoUI`에서 새로고침(뒤로가기→다시
      진입) 시 정확히 반영되는지, 반대로 디스코드에서 만든 문제가 웹 새로고침 시 보이는지

**UI 피드백 1차 4건 (2026-08-12 추가, 검증 완료)**
- [x] 문제 목록 각 행이 테두리 있는 카드(`.question-row`)로 보여 행 사이 경계가 뚜렷한지
- [x] 각 행에 유형 칩(주관식/O·X/객관식) + 문제·힌트·정답 각 구역의 내용 칩(텍스트/오디오/이미지 중
      채워진 것만)이 뜨는지(2차 피드백으로 "정답: {값}" 칩은 제거되고 q-summary 요약으로 대체됨 - 아래
      2차 항목 참고)
- [x] 행 전체 클릭으로도 편집 화면 이동(기존 동작 유지)이 되고, 별도의 ✏️ 수정 버튼도 동일하게
      동작하는지(⧉ 복제/🗑 삭제 버튼 클릭 시 행 이동은 발생하지 않는지도 확인)
- [x] 문제 편집 화면 상단에 지금까지 입력한 내용 요약 칩이 타이핑 즉시 갱신되는지
- [x] 문제 편집 화면 우측에 "👀 실제 디스코드에선 이렇게 보여요" 패널이 뜨는지, 탭을 ①기본정보/
      ②힌트설정/③정답공개로 옮길 때마다 미리보기가 문제 출제 중(노란 테두리)/힌트 공개(청록 테두리)/
      정답 공개(초록 테두리) 상태로 즉시 바뀌는지
- [x] ①기본정보 탭에서 문제 텍스트/오디오·이미지 URL을 입력하면 미리보기에 실시간 반영되는지, 정답
      유형을 O/X·객관식으로 바꾸면 미리보기 하단 버튼(⭕❌ 또는 1~5)도 같이 바뀌는지
- [x] ②힌트설정 탭에서 힌트 텍스트를 비워두면 정답 기반 자동 힌트(◼로 절반 가림)가 뜨는지, 정답도
      없는 새 문제에서는 "힌트를 만들 수 없어요" 안내가 뜨는지, 힌트 이미지 URL 입력 시 미리보기에도
      이미지가 뜨는지
- [x] ③정답공개 탭에서 정답값이 유형에 맞게 표시되는지(주관식은 입력 텍스트 그대로, O/X는 ⭕/❌
      이모지, 객관식은 "N번")

**UI 피드백 2차 9건 (2026-08-12 추가, 검증 완료)**
- [x] 문제 오디오 URL 입력 후 그 자리에 유튜브 플레이어(iframe)가 바로 뜨는지, 재생 버튼을 눌렀을 때
      "재생 시작(초)"에 입력한 지점부터 재생되는지(정답 공개 탭의 정답 오디오도 동일하게 확인) - 편집
      화면 폼과 우측 디스코드 미리보기 패널 양쪽 다 확인
- [x] 문제 목록 각 행에 "🗨 {정답 요약}"과 이미지/오디오가 있으면 작은 썸네일 아이콘이 뜨는지(오디오는
      유튜브 썸네일), 정답이 길면 말줄임(…) 처리되는지
- [x] URL 형식이 잘못된 문제, 또는 문제 텍스트/이미지/오디오가 전부 비어있는 문제(힌트나 정답 공개
      내용은 있어도)의 행이 연붉은 배경으로 표시되는지
- [x] `/editor` 목록에서 인증된 퀴즈 카드 썸네일 좌상단에 "✓ 인증" 배지가 뜨는지(디스코드에서 미리
      인증된 유저 퀴즈로 테스트, 유저 퀴즈 선택 웹 UI의 카드와 동일한 위치/스타일인지 비교)
- [x] 퀴즈 상세의 "다른 사람들에게 이렇게 보여요" 카드에서도 인증 마크가 썸네일 배지로 뜨는지(제작자
      줄에 텍스트로 안 섞여 있는지), 유저 퀴즈 선택 웹 UI의 상세보기 카드와 동일하게 보이는지
- [x] 문제 편집 중 정답 유형을 주관식→O/X→주관식 순서로 왔다 갔다 해도 주관식에 입력해둔 정답이
      그대로 복구되는지(O/X→객관식→O/X, 객관식→주관식→객관식 등 다른 조합도 확인)
- [x] 문제를 저장하면 퀴즈 상세로 안 돌아가고 이 화면에 그대로 머무는지, 저장 버튼이 잠깐 "✓ 저장됨"
      으로 바뀌는지. 새 문제를 처음 저장한 뒤 주소창 URL이 `/questions/:questionId`로 바뀌는지, 그
      상태에서 다시 저장하면 새 문제가 중복 생성되지 않고 같은 문제가 수정되는지(PUT 전환 확인)
- [x] breadcrumb에 "문제 수정 N/전체" 또는 "새 문제 추가 N번째로 추가" 칩이 뜨는지, 문제를 추가/삭제한
      뒤 숫자가 맞게 갱신되는지
- [x] 저장 버튼 옆 "← 이전 문제"/"다음 문제 →" 버튼이 뜨는지, 첫 문제에서 이전 버튼이, 마지막
      문제(또는 새 문제 추가 화면)에서 다음 버튼이 비활성화되는지, 클릭 시 폼이 해당 문제 내용으로
      바뀌는지, 저장 버튼(파란색)과 나머지 버튼(회색)이 시각적으로 구분되는지

## W. 퀴즈 만들기 웹 연동 — Phase 5 폴리시 + 통합 검증 (2026-08-12, 검증 완료)

`docs/plans/WEB_QUIZ_CREATION_PLAN.md` Phase 5. U/V 섹션과 달리 특정 화면이 아니라 인프라/엣지케이스
검증 항목.

- [x] 투트랙 선택 버튼(`/퀴즈`, `/퀴즈만들기`)의 라벨이 숫자가 아니라 "디스코드 UI"/"웹 UI" 텍스트로
      바로 보이는지
- [x] 2클러스터 이상 강제 실행 환경에서 `/퀴즈만들기` → 웹 UI 세션 생성/목록 로드/상세 진입이 정상
      동작하는지(브로드캐스트+로컬 조회 relay 패턴이 다중 클러스터에서도 성립하는지)
- [x] 웹 세션을 열고 브라우저 탭을 닫아 heartbeat를 중단한 뒤 15분 이상 지나면 디스코드 DM 잠금
      화면이 자동으로 해제되는지(GC 만료)
- [ ] "권한 가져오기"(force_take) 직후 새로 발급된 토큰이 곧바로 무효화되지 않는지 — 2026-08-12
      발견한 레이스 버그는 회귀 테스트(`web_session_manager.test.js`)로 재현+수정 확인 완료(코드
      레벨은 검증됨). **실제 두 유저가 같은 길드에서 경합하는 라이브 시나리오는 아직 안 해봄** —
      다음에 계정 2개로 직접 테스트 필요

## X. 나머지 디스코드 전용 화면 웹 포팅 — 안내/공지사항/서버 설정 (2026-08-12 구현, 실사용 검증 완료)

`docs/plans/WEB_UI_REMAINING_SCREENS_PLAN.md`. "퀴즈 선택 웹"(`/퀴즈` → 웹 UI, App.jsx) 헤더의 "☰ 더보기"
드롭다운 메뉴로 진입. 권한 체크는 의도적으로 없음(디스코드와 동일 동작).

- [x] 헤더의 "☰ 더보기" 버튼을 누르면 "🛠 퀴즈 만들기 안내"/"📢 공지사항"/"⚙️ 서버 설정" 3항목
      드롭다운이 뜨는지, 메뉴 바깥을 클릭하면 닫히는지
- [x] 브라우저 캐시/localStorage를 비운 상태로 처음 접속하면 "☰ 더보기" 버튼에 빨간 점(안 읽은 공지)
      배지가 뜨는지, 드롭다운의 "📢 공지사항" 항목 옆에도 같은 점이 뜨는지 — "📢 공지사항"을 클릭해
      들어갔다 나오면 두 배지가 모두 사라지고, 새로고침해도 다시 안 뜨는지(localStorage 저장 확인)
- [x] "🛠 퀴즈 만들기 안내"를 클릭하면 디스코드 `QuizToolGuideUI`와 동일한 제목/본문/문의사항/예시
      영상 안내가 뜨는지, "← 퀴즈 선택으로"를 누르면 원래 보던 탭으로 돌아가는지
- [x] "📢 공지사항"을 클릭하면 목록(`resources/notices/`의 파일들, 파일명 역순)이 뜨는지, 항목을
      클릭하면 본문이 뜨고 "← 목록으로"로 목록에 돌아갈 수 있는지, 본문에 `**굵게**` 같은 마크다운이
      정상 렌더링되는지(패치노트 항목은 스코프 아웃 — 목록에 파일이 있으면 그냥 공지처럼 뜨는 게 맞음)
- [x] "⚙️ 서버 설정"을 클릭하면 9개 옵션(노래 재생 시간/힌트·스킵 방식/유사 정답/점수 방식/정밀 오디오
      자르기/메시지 입력 감지/점수판 표시 인원/정답 제출 기회)이 현재 값으로 채워진 select로 뜨는지
- [x] 값을 하나 이상 바꾸면 "저장" 버튼이 활성화되고, 저장하면 "✅ 옵션을 저장했어요" 초록 배너가
      뜨는지(DB 저장 실패 시엔 "🛑 옵션 저장에 실패했어요" 빨간 배너가 대신 뜨는지도 가능하면 확인) —
      저장 없이 "← 퀴즈 선택으로"로 나가면 변경사항이 사라지는지(디스코드와 동일 동작)
- [x] 웹에서 서버 설정을 저장한 뒤, 같은 서버에서 디스코드 `/퀴즈` → 서버 설정 화면을 열어 웹에서
      바꾼 값이 그대로 반영돼 있는지(같은 `OptionStorage` 메모리 캐시 공유 확인) — 반대 방향(디스코드
      에서 바꾸고 웹에서 새로고침해 확인)도 같이 확인
- [x] 서버 옵션이 아무나 편집 가능한 상태(권한 체크 없음)가 의도한 대로인지 최종 확인(사용자 결정
      사항 — 회귀 아님)

## Y. 문제 미리보기 마크다운 지원 + 웹 API 보안 점검 (2026-08-12 구현, 코드/테스트 레벨만 검증 — 실사용 미검증)

`docs/plans/QUESTION_PREVIEW_AND_SECURITY_REVIEW_PLAN.md`. 보안 점검은 코드 리뷰+자동 테스트(`tsc`/`lint`/
`test` 343 pass)로 검증됨 — 아래 항목은 실제 Discord+브라우저로 아직 확인 안 함.

- [ ] 퀴즈 만들기 웹(`/퀴즈만들기` → 웹 UI)에서 문제 텍스트/힌트/정답 공개 문구에 `**굵게**`, `*기울임*`,
      `` `코드` ``, 줄바꿈(`\n` 한 번)을 섞어 입력하면 "실제 디스코드에선 이렇게 보여요" 미리보기에
      서식이 적용되고 단일 개행도 줄바꿈으로 보이는지 — 실제 문제 출제 시 디스코드 임베드와 비교해서
      서식이 일치하는지
- [ ] 힌트를 직접 입력하지 않고 정답만 입력해 자동 생성 힌트(◼ 가림)가 뜨는 경우, 마크다운 서식이
      적용되지 않고 그대로(원본 텍스트에 `*`/`` ` `` 등이 있어도 가공 없이) 보이는지
- [ ] 미리보기 영역이 라이트/다크 테마 토글과 무관하게 항상 디스코드 다크 테마 색으로 고정돼 보이는지
      (`.df-markdown`이 앱 테마에 물들지 않는지)
- [ ] 짧은 시간에 웹 API를 반복 호출(퀴즈 목록 새로고침 연타, 문제 저장 연타 등)해도 정상적인 사용
      패턴에서는 429(rate limited)가 뜨지 않는지 — 반대로 자동화 스크립트로 아주 빠르게 반복 호출하면
      일정 횟수 이후 429가 뜨는지(콘솔 네트워크 탭으로 확인)
- [ ] 서버 설정 저장/문제 저장 등에서 응답이 429일 때 프론트엔드가 에러 없이(빈 화면/무한 로딩 없이)
      적절히 실패로 처리하는지 — 429에 대한 전용 사용자 메시지는 이번 작업 스코프 밖(일반 실패 처리로
      충분한지 확인만)

**추가 심화 조사(같은 날, SQL 인젝션 발견+수정)로 새로 생긴 확인 항목**:

- [ ] 랜덤 퀴즈(오마카세)를 **퀴즈함(장바구니) 모드**로 실제 플레이해서 문제가 정상적으로 나오는지
      (디스코드 UI/웹 UI 둘 다) — `selectRandomQuestionListByBasket`의 SQL 호출 방식이 문자열 조립에서
      파라미터화 배열로 바뀌었으므로, 로직 자체가 깨지지 않았는지 실제로 한 번 돌려서 확인 필요
- [ ] 멀티플레이도 퀴즈함 모드로 로비를 만들어 실제 시작까지 진행해서 문제가 정상적으로 나오는지(같은
      코드 경로 공유)
- [ ] 웹에서 퀴즈 선택 중(확정 전) 다른 브라우저 탭이나 디스코드 화면에서 "현재 선택: ..." 미리보기가
      너무 길지 않게(60자 이내) 잘려서 보이는지 — 실사용에서 title이 60자를 넘는 경우가 실제로
      있는지도 참고로 확인(원래는 없어야 정상)

**같은 날 후속 수정(`basket_items` 소유권/공개여부 검사 추가)으로 새로 생긴 확인 항목**:

- [ ] 정상 흐름(퀴즈함에 공개 퀴즈만 담고 오마카세/멀티플레이 퀴즈함 모드로 플레이)이 `is_private =
      false`/`is_use = true` 필터 추가 후에도 그대로 정상 동작하는지 — 위 두 항목(퀴즈함 모드 실제
      플레이)과 같이 확인하면 됨, 별도 시나리오 아님

## Z. MMR 비대칭 보정 + 랜덤 추첨 셔플 버그 수정 (2026-08-12 구현, 코드/테스트 레벨만 검증 — 실사용 미검증)

`docs/plans/POST_B_ROUND_TEST_FEEDBACK_TODO.md` 11/12번. 자동 테스트(`tsc`/`lint`/`test` 347 pass)로만
검증됨 — 아래는 실제 Discord 멀티플레이로 아직 확인 안 함.

- [ ] 실제 멀티플레이 대결을 몇 판 진행해서 MMR 변동폭이 승/패 모두 체감상 자연스러운지(패배 시
      감소폭이 승리 시 증가폭 대비 너무 작게 느껴지지 않는지) — `calcLoserMMR`의 캡 제거로 풀게임
      패배 시 최대 -40점 → 최대 -80점까지 커짐, 극단적으로 과하게 느껴지진 않는지 확인
- [ ] 오마카세/디스코드 퀴즈/유저 퀴즈 각각 실제로 플레이해서 문제 순서가 매번 다르게 잘 섞이는지
      (`shuffleArray`로 교체된 3곳 — Dev/Custom/Omakase 퀴즈 초기화) — 특정 위치(맨 앞/뒤)에 같은
      문제가 몰리는 등 이상 패턴 없는지
- [ ] "특정 퀴즈 쏠림" 체감이 이번 셔플 수정만으로 해소됐는지, 아니면 여전한지(여전하면 근본 원인은
      `db_quiz.ts`의 `ORDER BY RANDOM()`이 퀴즈 단위가 아닌 문제 단위 균등 추출이라는 미해결 후보 —
      `docs/ACTIVE_PLAN.md` B-2 참고, 다음에 착수 검토)

## AA. 멀티플레이 밴 메시지 문구 개선 (2026-08-12 구현, 코드 레벨만 검증 — 실사용 미검증)

`docs/plans/UI_IMPROVEMENT_PLAN_ROUND2.md` B-5 마지막 [P1] 항목. `tsc`/`lint`/`test`(347 pass)로만 검증됨.

- [ ] 밴된 유저 계정으로 로비 생성/참가를 시도해서 "회원님이 ... 위반하여..." 메시지(+문의처)가 뜨는지
- [ ] 밴된 서버(길드ID)에서 로비 생성/참가를 시도해서 "이 서버가 ... 위반하여..." 메시지(+문의처)가
      뜨는지 — 위 유저 밴과 문구가 다르게 구분되는지 확인
- [ ] 밴되지 않은 정상 상태에서는 기존과 동일하게 로비 생성/참가가 막힘 없이 되는지(회귀 확인)

## AB. UI 개선 2라운드 [P2]/[P3] 일부 (2026-08-12 구현, 코드/테스트 레벨만 검증 — 실사용 미검증)

`docs/plans/UI_IMPROVEMENT_PLAN_ROUND2.md` B-1 전체 4항목 + B-4 2항목 + B-7 1항목. `tsc`/`lint`/`test`
(349 pass)로만 검증됨.

- [ ] `/도움말` 입력 시 안내 메시지가 정상적으로 뜨는지(ephemeral)
- [ ] 봇을 실제로 새 서버에 초대해서 `guildCreate` 환영 메시지가 뜨는지 — 시스템 채널이 있는 서버/
      없는 서버(또는 권한이 없는 서버) 둘 다 확인, 봇이 메시지를 보낼 채널이 아예 없을 때 에러 없이
      조용히 넘어가는지도 확인
- [ ] 화면이 만료된 버튼(오래된 UI, 봇 재시작 후 등)을 눌렀을 때 "이 버튼은 만료됐어요" 안내가
      뜨는지
- [ ] `/퀴즈정리` 입력 시 확인/취소 프롬프트가 먼저 뜨는지, "정리하기" 클릭 시 실제로 세션이
      정리되고 채널에 공개로 안내되는지("취소" 클릭 시엔 아무 일도 안 일어나는지)
- [ ] 서버 설정 화면에서 "기본값으로 초기화" 버튼을 눌렀을 때 옵션 설명이 기본값 기준으로 바뀌고,
      [저장]을 눌러야 실제로 DB에 반영되는지(초기화만으로는 반영 안 돼야 정상)
- [ ] 서버 설정 화면의 "유사 정답 생성 여부"/"정밀 오디오 자르기" 옵션 설명 문구가 새 문구로
      보이는지(오타 수정 포함)
- [ ] `/순위표`(스코어보드) 화면 제목이 "🎖 순위표 [베타 시즌]"로 기존과 동일하게 보이는지(문구
      출처만 `text_contents.json`으로 바뀌었을 뿐 화면상 텍스트는 변경 없음 — 회귀 확인)

## AC. "이 버튼은 만료됐어요" 오탐 + 실시간 공지 UI 이동 + 웹 세션 소유자 복귀 (2026-08-13 구현, 코드/테스트 레벨만 검증 — 실사용 미검증)

위 AB 섹션의 "화면이 만료된 버튼" 안내를 추가하며 생긴 회귀(uiHolder/quiz_session이 정상적으로
있어도 거의 모든 메뉴 전환에서 오탐 발생) 수정 + 곁가지 2건. `tsc`/`lint`/`test`(349 pass)로만
검증됨.

- [ ] `/퀴즈` → "디스코드 UI" 버튼 클릭 시 "이 버튼은 만료됐어요" 안내 없이 `MainUI`(기존 메인
      메뉴)로 정상 전환되는지
- [ ] `/퀴즈` → "웹 UI" 버튼 클릭 시 같은 안내 없이 웹 잠금 화면(`WebHandoffUI`)으로 정상 전환되는지
- [ ] `MainUI`의 1~5번 버튼(서버원과 게임하기/다른 서버와 경쟁하기/퀴즈 만들기/서버 옵션 설정/공지)을
      각각 눌러도 같은 안내 없이 정상 전환되는지(AB 섹션에서 이미 확인한 "진짜 만료된 버튼" 케이스는
      여전히 안내가 떠야 함 — 봇 재시작 직후 옛 메시지의 버튼을 눌러서 회귀 확인)
- [ ] `/퀴즈만들기` → "디스코드 UI"/"웹 UI" 버튼 둘 다 같은 안내 없이 정상 전환되는지
- [ ] `resources/current_notice.txt`에 내용이 있는 상태에서 `/퀴즈` 입력 시, 별도 메시지가 아니라
      "디스코드 UI/웹 UI 선택" 화면의 embed 안에 "📢 실시간 공지" 필드로 내용이 보이는지
- [ ] 위 상태에서 "웹 UI"를 선택해 잠금 화면으로 넘어간 뒤에는 공지가 다시 뜨지 않는지(웹 트랙에는
      공지가 불필요하다는 요청사항 확인)
- [ ] `current_notice.txt`가 비어있거나 없을 때는 "📢 실시간 공지" 필드 자체가 안 보이는지
- [ ] `/퀴즈` → "웹 UI"로 웹 세팅 화면(잠금 화면)을 띄운 채로, **같은 사람이** 다시 `/퀴즈`를 입력하면
      "권한 가져오기" 하이재킹 안내 없이 곧장 "디스코드 UI/웹 UI 선택" 화면이 새로 뜨는지(선택해서
      디스코드 UI로 정상적으로 돌아갈 수 있는지까지 확인)
- [ ] ~~위와 같은 상황에서 **다른 사람**이 `/퀴즈`를 입력하면 기존과 동일하게 "OOO 님이 웹에서 퀴즈를
      세팅하는 중입니다" + "권한 가져오기" 안내가 뜨는지~~ — **AD 섹션이 이 시나리오를 더 상세히
      대체함(785번 항목부터), 여기서는 스킵하고 AD로 진행할 것**
- [ ] `current_notice.txt`에 내용이 있을 때, "디스코드 UI/웹 UI 선택" 화면에서 설명 문구와 "📢 실시간
      공지" 필드 사이에 구분선(가로줄 필드)이 보이는지

## AD. 하이재킹 방어가 디스코드 UI 트랙엔 없던 근본 버그 수정 + force_take 재설계 (2026-08-13 구현, 코드/목 스크립트 레벨만 검증 — 실사용 미검증)

AC 섹션 배포 직후 사용자가 실제로 재현해서 발견한 버그 — 디스코드 UI 트랙(`MainUI`)엔 하이재킹
가드가 아예 없어서, 사용자1이 디스코드 UI로 진행 중일 때 사용자2가 `/퀴즈`를 입력하면 확인 없이
곧장 화면을 가로챌 수 있었음(사용자1의 진행 상황이 조용히 사라짐). 위 AC 섹션의 "🔓 권한을
가져왔어요" 안내/토큰 재사용 관련 항목은 이번 재설계로 **더 이상 유효하지 않음**(아래로 대체) —
`tsc`/`lint`/`test`(349 pass)로만 검증됨, 목(mock) 스크립트로 시나리오 흐름은 직접 재현 확인함.

- [ ] 사용자1이 `/퀴즈` → **"디스코드 UI"**를 선택해 `MainUI`(또는 그 하위 화면)를 쓰는 중일 때,
      **사용자2**가 `/퀴즈`를 입력하면 곧장 새 화면이 뜨지 않고 "🔒 사용자1 님이 퀴즈 화면을 사용하는
      중입니다" + "🔓 권한 가져오기" 안내(ephemeral, 사용자2에게만 보임)가 뜨는지 — 이게 이번에
      새로 고친 핵심 버그
- [ ] 사용자1이 `/퀴즈` → **"웹 UI"**를 선택해 웹 잠금 화면을 쓰는 중일 때 사용자2가 `/퀴즈`를 입력하면
      "🔒 사용자1 님이 웹에서 퀴즈를 세팅하는 중입니다" 안내가 뜨는지(문구가 트랙에 따라 다르게
      나오는지 확인 — 회귀 확인, AC 섹션과 동일 케이스)
- [ ] 위 두 경우 모두, 사용자2가 "🔓 권한 가져오기"를 누르면 사용자1의 화면(메시지)은 그대로 남아있고
      (강제로 지워지지 않음), 사용자2에게 새로운 "디스코드 UI/웹 UI 선택" 화면이 공개 메시지로 뜨는지
      (권한 가져오기 버튼을 누른 채널에)
- [ ] 권한을 가져온 뒤 그 새 두 트랙 화면에서 사용자2가 "웹 UI"를 선택하면 정상적으로 새 웹 세션이
      발급되고 잠금 화면으로 넘어가는지, "디스코드 UI"를 선택하면 `MainUI`로 정상 진입하는지
- [ ] 권한을 가져온 뒤, 사용자1이 예전 화면(이미 넘겨준 화면)의 버튼을 눌러보면 "해당 UI를 생성한
      사용자2님만 조작할 수 있어요" 안내가 뜨는지(소유권이 실제로 넘어갔는지 확인)
- [ ] "권한 가져오기"를 아무도 안 누른 상태에서 사용자1이 자기 화면을 계속 정상적으로 조작할 수
      있는지(하이재킹 안내가 뜬 것 자체가 사용자1 쪽에는 아무 영향이 없어야 함)
