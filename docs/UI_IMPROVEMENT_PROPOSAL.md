# UI 개선안 (퀴즈만들기 + 사용자 UI 전반)

> 2026-08-05, `convertTagsValueToString`의 trailing comma 버그(태그 2개 이상 선택 시 "음악 퀴즈, 그림 퀴즈, "처럼 끝에 콤마가 남던 문제, `fix:` 커밋으로 이미 수정됨)를 계기로, 퀴즈만들기(`/퀴즈만들기`)와 그 주변 사용자 UI 전반의 가독성/일관성을 검토한 결과 기록.
>
> **성격이 다른 기존 문서와의 관계**: `DUPLICATE_UI_PATTERNS.md`는 코드 중복(같은 컴포넌트를 여러 곳에서 반복 정의) 관점, `BUGS_FOUND.md`는 로직 버그 관점이고, 이 문서는 **사용자가 실제로 보는 텍스트/버튼/흐름의 가독성·일관성·기대 불일치**만 다룬다. 코드 구조는 멀쩡한데 문구/라벨/색상 선택이 사용자에게 혼란을 주는 경우가 대상.
>
> 진행 상황(2026-08-06 기준): **0/1/2/3/4/5번 모두 수정 완료** (1번의 "옵션 설명 비대칭"만 콘텐츠 작성이 더 필요해 이월). 6번은 참고용으로 남겨둠.

---

## 0. 즉시 수정 권장 (명백한 오타/버그, 각 1줄 수정) — ✅ 수정 완료 (2026-08-06)

이번 comma 버그와 같은 성격 — 판단이 필요 없는 명백한 실수라 바로 고쳐도 리스크가 없음.

1. **`quizbot/quiz_ui/components/custom_quiz_components.js:244`** — 문제 이미지 URL 필드 라벨의 대괄호가 안 닫혀 있고 불필요한 마침표가 붙어 있음:
   ```
   '문제와 함께 표시할 이미지입니다. [.WebP 형식은 사용 불가'
   ```
   힌트 이미지(`:285`)/정답 공개 이미지(`:350`) 필드는 정상 형식(`'... [WebP 형식은 사용 불가]'`)이라 셋을 통일해야 함. → `'... [WebP 형식은 사용 불가]'`로 통일.

2. **`quizbot/quiz_ui/components/custom_quiz_components.js:421`** — "주관식" 답변 유형 설명에 오타: `"플레이어는 메세지로 정답을 입력하는 방식입니다."` → "메시지"로 수정.

3. **`quizbot/quiz_ui/user-quiz-info.ui.js`** (삭제 완료 메시지) — `"퀴즈를 삭제했습니다.\r삭제한 퀴즈 ID: " + quiz_id` 에서 `\r`을 `\n`으로 수정하고, `quiz_id` 대신 퀴즈 제목을 보여주도록 변경(`user-quiz-list-ui.js:221`의 "수정 권한 없음" 메시지도 동일하게 quiz_id 노출 제거). 삭제 확인 프롬프트(`confirm_quiz_delete`)에도 대상 퀴즈 제목을 함께 표시하도록 추가.

---

## 1. 용어/라벨 일관성 — 대부분 수정 완료 (2026-08-06)

같은 개념을 화면마다 다른 단어로 부르거나, 같은 단어를 다른 개념에 재사용하는 사례.

- ✅ **공개/비공개 토글 버튼** (`custom_quiz_components.js:29`, `quiz_toggle_public`) — 라벨이 항상 `'퀴즈 공개/비공개'` 고정이라, 지금 상태가 뭔지·눌렀을 때 어떻게 바뀌는지 버튼만 봐서는 알 수 없었음. → 라벨이 "누르면 바뀔 상태"를 직접 보여주도록 변경(비공개 상태면 `'🔓 퀴즈 공개로 전환'`, 공개 상태면 `'🔒 퀴즈 비공개로 전환'`). 이 버튼은 롱리브드 싱글턴(`quiz_edit_comp`)이라 `user-quiz-info.ui.js`의 `refreshUI()`에서 매번 `cloneDeep()`한 뒤 라벨을 바꿔치기함(원본을 직접 mutate하면 다른 유저의 편집 세션에도 영향을 주므로). 제목 배지도 `[비공개🔒]`(아이콘을 텍스트 앞으로) 순서 통일.
- ✅ **"인증"(certified) 개념** — `user-quiz-info.ui.js:147`(인증여부 ⭕/❌), `quiz-info-ui.js:128`(인증 필터), `omakase_components.js`/`multiplayer_components.js`(인증된 퀴즈 필터 모달, 총 3곳)에 반복 등장하지만 설명이 없었음. → `feedback_manager.js`/`SYSTEM_CONFIG.CERTIFY_LIKE_CRITERIA`(=10) 기준을 확인해, 5곳 모두에 "추천 10개↑" 조건을 짧게 병기.
- ✅ **뒤로가기 버튼 라벨 3종** — `base_components.js`의 `control_btn_component`만 `'뒤로'`(2글자)였던 걸 다른 화면과 동일한 `'뒤로가기'`로 통일. 같은 컴포넌트의 `'점프'` 버튼도 그 버튼이 여는 모달 제목("페이지 이동")과 맞춰 `'페이지 이동'`으로 수정. (`custom_quiz_components.js:461`의 `btn_done`은 확인해보니 장바구니 담기 모드에서만 `'뒤로'` 버튼을 갈아끼우는 의도된 분기(`user-quiz-select-ui.js:71`, "완료"=장바구니 선택을 마쳤다는 의미)라 그대로 둠 — 오해였음.)
- ✅ **"퀴즈태그" vs "퀴즈 태그"** — `user-quiz-info.ui.js:149`의 붙여쓰기를 다른 곳과 같은 띄어쓰기로 통일.
- ✅ **"검색용 퀴즈 태그"라는 라벨의 오용** — `custom_quiz_components.js`의 `quiz_tags_select_menu`(퀴즈 편집 화면에서 태그를 붙이는 용도)의 placeholder가 `'검색용 퀴즈 태그 선택하기'`였던 걸 `'퀴즈에 붙일 태그 선택하기 (여러 개 선택 가능)'`로 수정.
- ✅ **"정답 이벤트 설정" 버튼 → "문제 정답 공개 시 설정" 모달** — 버튼 라벨을 모달 제목과 맞춰 `'정답 공개 설정'`으로 수정.
- ✅ **오마카세 장바구니 select 3종 동일 placeholder** — 실제로 항목을 제거하는 `omakase_basket_select_menu`만 placeholder를 `'선택하여 장바구니에서 제거하기'`로 수정(읽기 전용 변형인 `omakase_basket_readonly_select_menu`는 "확인하기"가 맞는 표현이라 그대로 둠). 겸사겸사 `quiz-info-ui.js`의 옵션 `description: '.'`(점 하나만 있던 플레이스홀더)도 제거.
- **옵션 설명 유무의 비대칭** — `option_component`/`question_answer_type_select_menu`처럼 각 선택지마다 한 줄 설명이 달린 select가 있는 반면, 태그 선택 계열 select(`quiz_tags_select_menu`, 오마카세 태그 select 전부)는 라벨만 있고 설명이 전혀 없음. QUIZ_TAG/DEV_QUIZ_TAG 태그마다 의미 있는 한 줄 설명을 새로 써야 하는 콘텐츠 작업이라 **아직 미착수** — 다음 라운드로 이월.

---

## 2. 파괴적 동작에 대한 안전장치 불균형 — ✅ 수정 완료 (2026-08-06)

- **문제(질문) 단위 삭제 vs 퀴즈 전체 삭제** — 퀴즈 삭제는 확인 화면(취소/삭제 2버튼, `quiz_delete_confirm_comp`)을 거치는데, `question_edit_comp2`(`custom_quiz_components.js:399~411`)의 **"현재 문제 삭제"는 확인 절차 없이 원클릭으로 즉시 삭제**됨. 둘 다 되돌릴 수 없는 삭제인데 보호 수준이 다름. → `user-question-info-ui.js`에 확인 대기 상태(`pending_question_delete_confirm`)를 추가해, 같은 버튼을 한 번 더 눌러야 실제로 삭제되는 2클릭 방식으로 변경. 다른 버튼을 누르면 확인 대기가 자동 해제됨(오클릭 시 실수로 다음 클릭에 삭제되는 것 방지).
- **"삭제 + 제작자 영구밴" 버튼의 시각적 무게** (`custom_quiz_components.js:104~122`, `quiz_delete_confirm_admin_comp`) — "삭제만" 버튼과 "삭제+영구밴" 버튼이 나란히 **같은 `ButtonStyle.Danger`(빨강)**로 배치돼 있음. 콘텐츠 삭제와 계정 영구밴은 되돌리기 난이도가 전혀 다른 결정인데 색으로 구분이 안 됨. → Discord 버튼 스타일에 Danger보다 더 강한 단계가 없어 색 자체는 유지하되, "삭제+영구밴" 버튼을 별도 행으로 분리하고 라벨에 "⚠️ ... (해제 전까지 되돌릴 수 없음)" 경고 문구를 추가해 시각적/공간적으로 구분.
- **삭제 확인 프롬프트에 대상 정보 없음** — `user-quiz-info.ui.js:333`의 삭제 확인 문구("정말 해당 퀴즈를 삭제할까요?")와, 이후 다른 화면들의 오류 메시지(`user-quiz-list-ui.js:221` "당신은 해당 퀴즈를 수정할 권한이 없습니다. quiz_id: 123")가 **퀴즈 제목이 아니라 내부 quiz_id를 노출**함. 사용자가 실제로 기억하는 건 제목이지 숫자 ID가 아님. → 둘 다 quiz_id 대신 퀴즈 제목을 보여주도록 수정(0번 항목과 함께 처리).

---

## 3. 임베드 텍스트 구조/가독성 — ✅ 수정 완료 (2026-08-06)

- ✅ **`user-quiz-info.ui.js`의 상세 정보 임베드**가 볼드/백틱/수동 줄바꿈(`\n\n\n` vs `\n\n\n\n`)을 섞어 만든 하나의 긴 문자열이었음. → 통계성 항목(만들어진 날짜/업데이트 날짜/플레이한 서버/추천한 유저수/인증여부/퀴즈 태그)을 embed `fields` 배열로 분리해 표 형태로 보이도록 구조화. `description`에는 제작자/한줄소개/문제개수/퀴즈 설명 같은 서술형 내용만 남김. 태그가 하나도 없을 때 필드 값이 빈 문자열이 되면 Discord API가 거부하므로 `'선택 안함'` 폴백 추가.
- ✅ **조사(단위) 오용** — `플레이한 서버: X개`(서버 수), `추천한 유저수: X개`(사람 수) → 각각 "곳"/"명"으로 수정(1번 항목 작업 때 함께 처리됨).
- **`quiz-info-ui.js`의 `getTagInfoText`** — 어느 화면에서 호출되든 "📕 공식 퀴즈 설정"과 "📗 유저 퀴즈 설정" 두 블록을 항상 같이 보여주는 것에 대해, 실제 호출부(오마카세 방/멀티플레이 로비)를 확인해보니 **둘 다 공식+유저 퀴즈 풀을 동시에 섞어서 출제하는 "오마카세" 특성상 의도된 동작**이었음. 두 소스를 동시에 쓰는 화면이라 둘 다 보여주는 게 맞고, 괜히 조건부로 숨기면 오히려 "선택 안 함" 같은 유효한 상태 정보를 감추는 셈이라 **수정하지 않음**(투자 대비 실익이 없다고 판단).
- ✅ **모달 필드 라벨에 글자 수 제한이 전혀 언급되지 않음** — 퀴즈 제목(`minLength(4)`/`maxLength(40)`)과 검색어(`maxLength(10)`) 라벨에 "(4~40자)"/"(최대 10자)" 힌트를 추가. 페이지 이동처럼 4자리 숫자로 자명한 필드는 그대로 둠. 겸사겸사 같은 모달 안에서 다른 필드들만 마침표로 끝나던 것과 통일해 "퀴즈를 자유롭게 소개해주세요." 뒤에 마침표 추가.

---

## 4. 흐름/사용자 기대 불일치 — ✅ 수정 완료 (2026-08-06)

- ✅ **메인 메뉴 "3️⃣ 퀴즈 만들기"를 눌러도 바로 만들기가 시작되지 않음** — `QuizToolGuideUI`(안내 화면)로 이동해서 "`/퀴즈만들기` 명령어를 입력해주세요"라는 문구만 보여줘서, 왜 버튼을 눌렀는데 또 명령어를 입력해야 하는지 이유가 안 보였음. → "퀴즈 제작 툴은 개인 메시지(DM)에서 진행돼요"라는 이유를 먼저 설명하도록 안내 문구 수정(구조는 그대로 — DM 유도라는 아키텍처를 바꾸는 대신 왜 이런 흐름인지 납득시키는 쪽을 선택).
- **"자유 텍스트로 예/아니오 표현" 패턴이 최소 3곳(실제로는 중복 모달까지 5곳) 반복** — 인증 필터 끄기, 정답 대기시간 사용 여부 모두 `placeholder: '예시) 네'`처럼 "네"를 예시로 보여주면서 실제로는 아무 문자나 입력하면 true로 처리되던 문제. → select/버튼 토글로 완전히 바꾸는 건 모달 구조(3개 모달이 `customId` 공유) 자체를 손대야 하는 큰 변경이라 이번엔 보류하고, **판정 로직만 화이트리스트 방식으로 강화**(`'네'`/`'예'`/`'ㅇ'`/`'y'`/`'Y'`처럼 명확한 긍정 응답일 때만 true로 처리, 스페이스나 오타 입력 시 안전한 기본값 유지). node -e로 empty/space/정상값 케이스를 직접 시뮬레이션해 검증함.
- ✅ **"최근 장바구니 불러오기" 동작이 불명확** — 실제 핸들러(`omakase-quiz-room-ui.js`/`multiplayer-quiz-lobby-ui.js`의 `handleLoadBasketItems`) 코드를 확인해보니 병합이 아니라 **완전히 덮어쓰기**하는 동작이었음. → 버튼 라벨을 `'최근 장바구니로 덮어쓰기'`로 수정해 동작을 명확히 함.
- ✅ **`option_control_btn_component`의 "뒤로가기" 버튼이 빨간색** — 저장 안 하고 나가면 변경사항이 사라지는 걸 암시하는 의도로 보였으나 문구가 없었음. → `text_contents.json`의 서버 설정 화면 description 끝에 "저장하지 않고 뒤로가기를 누르면 변경사항이 사라집니다" 경고문 추가(색상 자체는 의도된 경고 신호라 그대로 유지).

---

## 5. 데이터 정확성 문제 (텍스트가 사실과 다른 값을 보여줌) — ✅ 수정 완료 (2026-08-05)

- **`quizbot/quiz_ui/user-quiz-list-ui.js:238~270` (`sendQuizPlayedInfo`)** — 퀴즈 목록을 열 때마다 DM으로 보내는 안내문 중 `"이번 주에 가장 플레이된 퀴즈는 [...]이네요!"`가 `best_quiz`(전체 기간 1위)와 **완전히 동일한 `played_count`(전체 누적치) 필드**를 비교해서 뽑고 있었음.
  - 실제로는 DB에 주간 집계용 `played_count_of_week` 컬럼이 별도로 존재하고(`db_quiz.js`의 조회수 증가 쿼리에서 `played_count`와 함께 매번 갱신됨, `UserQuizInfo` 데이터에도 포함됨), **라이브 서버에서 매주 월요일 09시 cron으로 이 필드를 초기화**하고 있음 — 즉 주간 집계 인프라 자체는 이미 갖춰져 있었고, 이 화면 코드만 `played_count_of_week` 대신 실수로 `played_count`를 두 번 비교하고 있었던 단순 오타성 로직 버그.
  - `best_quiz_of_week` 비교 기준을 `quiz_info.data.played_count_of_week`로 수정 완료.

---

## 6. 낮은 우선순위 관찰 (당장 손 안 대도 되는 것들)

- 메인 메뉴 제목 `"🔔 퀴즈봇 [3]"`(`main-ui.js`)의 `[3]`이 사용자에게 아무 의미도 전달하지 못함(추정: 내부 버전/샤드 마커).
- `main_ui_component`의 "봇 공유" 버튼(`base_components.js:153~163`)이 실제로는 초대 링크가 아니라 koreanbots.dev(외부 투표 사이트)로 이동함 — 라벨만 보면 봇 초대 링크로 오해할 수 있음.
- `text_contents.json`의 `main_menu.description`이 실제로는 `main-ui.js`가 자체적으로 만드는 하드코딩된 설명(대기 중 로비 수 포함)에 밀려 렌더링에 전혀 안 쓰이는 죽은 문구로 보임 — 나중에 이 JSON만 고치고 "왜 안 바뀌지?" 헤맬 수 있음.

---

## 권장 진행 순서

1. ~~**0번(오타 3건)**~~ — 수정 완료.
2. ~~**5번(주간 1위 데이터 오류)**~~ — 수정 완료.
3. ~~**2번(파괴적 동작 안전장치)**~~ — 수정 완료.
4. ~~**1번(용어/라벨 일관성)**~~ — "옵션 설명 비대칭" 항목만 남기고 수정 완료.
5. ~~**3번(임베드 텍스트 구조/가독성)**~~ — `getTagInfoText` 항목은 조사 후 의도된 동작으로 판단해 그대로 둠, 나머지 수정 완료.
6. ~~**4번(흐름/기대 불일치)**~~ — "자유 텍스트 예/아니오" 항목은 판정 로직 강화로 대체 처리, 나머지 수정 완료.
7. **6번**은 굳이 지금 안 고쳐도 됨 — 참고용 기록.

남은 건 1번의 "옵션 설명 비대칭"(태그 select 메뉴마다 옵션 설명 추가 — QUIZ_TAG/DEV_QUIZ_TAG 태그 수만큼 의미 있는 문구를 새로 써야 하는 콘텐츠 작업)과, 4번에서 보류한 "자유 텍스트 예/아니오 → select/버튼 토글 전면 전환"(모달 구조 자체를 바꿔야 해서 `customId` 공유 등 기존 제약과 맞물림) 정도가 남았습니다. 둘 다 지금 방식대로도 실사용에 큰 문제는 없어서 급하지 않다고 판단해 이번 라운드에서는 제외했습니다.
