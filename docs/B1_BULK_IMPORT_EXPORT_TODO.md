# B-1. 문제 일괄 등록 (Export/Import) — 미결정 사항

> `TS_MIGRATION_AND_CONVENIENCE_PLAN.md`의 B-1 항목 착수 전 확정해야 할 설계 결정 3가지를
> 2026-08-07 논의 중 사용자 요청으로 이 문서로 분리함(당장 결정하지 않고 보류). B-1 착수 시
> 이 문서부터 다시 열어서 하나씩 확정할 것.

## 1. JSON 필드명

문제 데이터를 JSON으로 Export/Import할 때 필드명을 어떻게 정할지.

- **옵션 A (추천)**: DB 컬럼명 그대로 사용(`question_audio_url`, `answers`, `audio_start` 등).
  변환 계층이 필요 없고, `applyQuestionInfo` 등 기존 로직을 그대로 재사용 가능.
- **옵션 B**: 사람이 읽기 쉬운 이름으로 별도 매핑(예: "정답", "문제_오디오_URL").
  파일을 직접 열어봤을 때 이해하기 쉽지만, 컬럼명이 바뀔 때마다 매핑도 같이 유지보수해야 함.
- `quiz_id`/`question_id`는 어느 옵션이든 export에서 제외하고, import 시 현재 열려있는 퀴즈의
  값으로 자동 덮어쓰는 것으로 합의됨(이 부분은 확정, 재논의 불필요).

## 2. Import 시 기존 문제 처리 방식

- **옵션 A (추천)**: 항상 "추가"만 허용(교체 기능 자체를 만들지 않음). 파괴적 동작을 원천 차단 —
  전부 교체가 필요하면 유저가 기존 문제를 직접 지운 뒤 import.
- **옵션 B**: "추가"/"전부 교체" 두 모드 제공. 교체는 B-3'에서 이미 쓴 2클릭 확인 패턴을
  재사용해 실수 방지. 구현 범위가 늘어남.

## 3. 검증 실패 시 에러 메시지 상세도

- **옵션 A (추천)**: 실패한 문제 목록 전체를 한 번에 표시(예: "3번째 문제: answers 필드 누락",
  "7번째 문제: 정답 형식 오류"). 유저가 파일을 한 번에 고칠 수 있음.
- **옵션 B**: 첫 번째 에러만 표시. 구현은 단순하지만, 여러 군데 문제가 있으면 유저가 여러 번
  재시도해야 함.
