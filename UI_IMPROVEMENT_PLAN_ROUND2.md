# UI/편의성 개선 작업계획서 (2라운드)

> `UI_IMPROVEMENT_PROPOSAL.md`(1라운드, 퀴즈만들기 중심)가 완료된 뒤, 조사 범위를 봇 전역으로 넓혀서
> Explore 서브에이전트 2개(병렬)로 다음 영역을 훑은 결과. **아직 아무 것도 수정하지 않은 순수 계획 문서**다.
>
> 조사 범위: 봇 전역 에러/타임아웃 처리, 온보딩/도움말 유무, 신고 제출 플로우, 추천(좋아요) 피드백,
> 서버 설정 옵션 문구, 멀티플레이 로비 목록/내부, 스코어보드, 공지사항, 퀴즈 유형 선택/공식 퀴즈 브라우저,
> 관리자 패널(`/quizmgr`).
>
> 이번에도 성격이 다른 기존 문서와는 구분: `BUGS_FOUND.md`(로직 버그), `DUPLICATE_UI_PATTERNS.md`(코드 중복),
> `UI_IMPROVEMENT_PROPOSAL.md`(1라운드, 퀴즈만들기 UX). 이 문서는 **A. 이번에 새로 발견한 실제 버그**(우연히
> UX 조사 중 발견된 것들)와 **B. 화면별 UX/편의성 개선 후보**로 나눈다.

---

## A. 조사 중 발견한 실제 버그 (우선순위 최상 — UX 문제가 아니라 동작 오류)

1. **`scoreboard-ui.js` — 스코어보드 조회 실패 시 화면이 "불러오는 중..."에서 영원히 멈춤.**
   `selectTop10Scoreboard()`가 `undefined`/빈 결과를 반환하는 분기에서 `this.embed.description`만 바꾸고 `this.update()`를 호출하지 않고 `return`함(정상 경로에서만 마지막에 `update()` 호출). 즉 실패해도 사용자 화면은 초기 로딩 placeholder에 그대로 멈춰있고, 에러 표시도 재시도 방법도 없음.
2. **`multiplayer-quiz-select-ui.js`의 `handleRequestRefreshLobby` — 새로고침이 끝나기 전에 "다시 불러왔습니다" 응답이 감.** `this.loadMultiplayerLobbyList()`(비동기, IPC 왕복 포함)를 `await` 안 하고 바로 확인 메시지를 보냄 — 실제 목록 갱신보다 "완료" 메시지가 먼저 도착할 수 있음.
3. **`feedback_manager.js`의 `addQuizLike`/`addQuizLikeAuto`에 `.catch()`가 없음.** DB 에러/네트워크 문제로 추천 처리가 실패하면 프라미스 체인이 그냥 끊기고 사용자에게는 **아무 응답도 안 감** — 버튼을 눌렀는데 반응이 없는 것처럼 보임.
4. **봇 전역에 `unhandledRejection` 핸들러가 없음** (`bot.js`는 `uncaughtException`만 있고, 그마저도 `'Status code:'` 에러만 특별 처리). UI 핸들러(`quiz_ui/*.js`)에서 예기치 못한 예외가 나면 사용자에게 어떤 안내도 없이 그냥 무반응 — 위 3번과 같은 "조용한 실패" 패턴이 구조적으로 반복될 수 있는 원인.
5. **`select-quiz-type-ui.js` — 버튼 "4"/"5"가 화면에 보이지만 눌러도 아무 동작 안 함.** `select_btn_component`가 1~5번 버튼을 렌더링하는데 핸들러는 1~3만 처리(실제 옵션은 3개뿐). 죽은 버튼 2개가 계속 노출됨.
6. **`multiplayer-quiz-lobby-ui.js`의 `multiplayer_participant_select_menu`(비호스트용 "참여 중인 서버 목록 확인")에 핸들러가 없음.** select menu처럼 보이지만 선택해도 아무 반응 없는 죽은 컨트롤.
7. **`note-select-ui.js`의 `isNoteSelectUIEvent`에 `return`이 빠져 있어 항상 `undefined` 반환.** 다만 이 함수가 처리하려던 `'notice'`/`'patch_note'` 버튼 자체가 이미 삭제된 상태(`DEPRECATED_CODE_REMOVED.md`)라 현재는 도달 불가 코드라 실제 피해는 없음 — 죽은 코드 정리 대상으로 분류.

**권장**: 1~3번은 각각 한 함수 안에서 끝나는 국소적 수정이라 리스크 낮음 — 다음에 바로 고쳐도 될 후보. 4번(전역 unhandledRejection 핸들러)은 다른 실패들을 근본적으로 덜 "조용하게" 만들 수 있는 구조적 개선이라 별도로 설계 검토 필요. 5, 6, 7번은 죽은 버튼/코드 정리 성격.

---

## B. 화면별 UX/편의성 개선 후보

우선순위 태그: **[P1]** 사용자가 자주 마주치는 혼란/불편, **[P2]** 있으면 좋음(편의 기능), **[P3]** 참고용(고치지 않아도 무방).

### B-1. 봇 전역 에러/온보딩

- **[P2] 도움말/온보딩이 전혀 없음.** `/도움말` 같은 명령어 없음, 봇이 새 서버에 들어갔을 때 안내 메시지(`guildCreate` 리스너) 없음. 새 유저는 Discord가 보여주는 슬래시커맨드 한 줄 설명이 안내의 전부.
- **[P2] 인터랙션 타임아웃이 완전히 침묵.** 만료된 버튼을 누르면 `bot.js`가 `deferUpdate()`만 시도하고 실패하면 조용히 무시 — "이 버튼은 만료됐어요, 메뉴를 다시 열어주세요" 같은 안내가 없음.
- **[P3] `/퀴즈정리`(강제 세션 정리)가 확인 절차 없이 즉시 전체 세션을 지움.** 관리 목적 명령어라 실사용 빈도는 낮지만, 진행 중인 사람이 있는지 미리 보여주지 않음.
- **[P3] 모든 봇 메시지가 🔸 하나로 정보/에러/성공을 뭉뚱그려 표현.** 심각도별 구분이 이모지 선택에만 의존.

### B-2. 신고 플로우 (`report_components.js`, `report_submission.js`)

- **[P1] 신고 접수 후 "그 다음에 어떻게 되는지"에 대한 안내가 전혀 없음.** "신고가 접수되었습니다. 감사합니다." 한 줄이 끝 — 처리 기간, 결과 통보 여부(사실 신고자에게는 결과가 통보되지 않음) 등 기대치 설정이 없음.
- **[P1] 실패 메시지에 내부 구현 용어가 그대로 노출됨.** `"신고에 실패했습니다. (No Cache Content)\n다시 시도해보세요."` — 영어 내부 키가 사용자 메시지에 섞여 있고, 왜 실패했는지(5분 캐시 만료) 설명이 없음.
- **[P2] 신고 모달 제목이 "신고ID"** — 내부 디버그 라벨처럼 보임. "채팅 신고하기" 등으로 바꾸는 게 자연스러움. 사유 입력 필드에 placeholder도 없음(글자 수 제한 100자 안내도 없음).
- **[P3] 신고 관련 문구가 `text_contents.json`에 전혀 없고 전부 하드코딩** — 다른 화면과 다른 컨벤션(추적/수정하기 불편).

### B-3. 추천(좋아요) 피드백 (`feedback_manager.js`)

- **[P2] 추천 수가 어디에도 표시되지 않음.** 인증 기준(추천 10개)에 쓰이는 값인데 사용자는 지금 몇 명이 추천했는지 전혀 볼 수 없음(퀴즈 정보 화면에 필드 하나 추가하면 해결).
- **[P3] "방금 추천 성공" / "이미 추천했음" 두 상태가 이모지(👍 vs 💚)로만 구분됨.** 문구 자체는 비슷해서 두 상태를 명확히 구분하기 어려움.
- **[P3] 좋아요 버튼이 누른 후에도 계속 클릭 가능한 상태로 남아있음** — 비활성화나 체크 표시 없음.

### B-4. 서버 설정 (`server-setting-ui.js`, `text_contents.json`)

- **[P1] 옵션 값 저장 확인 토스트가 9개 옵션 중 6개에서 사람이 읽을 수 없는 raw 값을 보여줌.** 예: 노래 재생시간을 "30초"로 골랐는데 토스트는 "옵션 값을 30000로 설정했습니다."(ms 단위), on/off 옵션은 "true"/"false", 무제한 옵션은 "-1". 정작 임베드 설명에는 올바르게 번역돼 있어서 토스트와 화면이 서로 다른 표현을 보여주는 셈.
- **[P2] "기본값으로 초기화" 기능이 없음.** 실수로 다 바꾼 뒤 되돌리려면 기본값을 기억해서 하나하나 다시 선택해야 함.
- **[P3] "유사 정답 생성" 설명이 예시 하나(크레이지 아케이드→크아)에 의존** — 그 게임을 모르면 기능을 이해하기 어려움. "정밀 오디오 자르기" 설명에 "하이트라이트"(하이라이트 오타)도 있음.
- **[P3] "메시지 입력 감지" 옵션 설명에 껐을 때 `/답` 명령어가 필요해진다는 트레이드오프가 안 적혀있음**(값 목록까지 들어가야 보임).

### B-5. 멀티플레이 — 로비 목록 (`multiplayer-quiz-select-ui.js`)

- **[P1] 개인적인 실패(이미 게임 중/이미 참여 시도 중)가 채널 전체에 공개로 전송됨** (`interaction.channel.send`) — 다른 대부분의 실패 메시지는 ephemeral인데 이 두 개만 공개로 나감.
- **[P1] 밴 메시지가 "당신 또는 이 서버가..."로 시작해 본인이 밴된 건지 서버가 밴된 건지 구분이 안 되고, 다음 행동(이의제기 등) 안내가 없음.**
- **[P2] 로비가 없을 때도 번호 버튼(1~10)+페이지네이션 버튼이 그대로 다 떠있음.** 눌러도 반응 없는 죽은 버튼들이 "로비가 없어요" 메시지와 함께 표시됨.
- **[P2] 검색 기능 관련 import(`btn_search`, `modal_complex_page_jump`)만 있고 실제로 연결 안 돼 있음** — 로비가 많아지면 이름으로 찾을 방법이 없음(페이지 넘기기만 가능).
- **[P3] "시작할 수 없습니다" 문구(`quiz_info_ui.failed_start`)를 로비 생성/참가 전 단계에도 재사용** — 아직 시작 버튼을 누르지도 않았는데 "퀴즈를 시작할 수 없습니다"라고 나옴(맥락 불일치).

### B-6. 멀티플레이 — 로비 내부 (`multiplayer-quiz-lobby-ui.js`, `multiplayer_components.js`)

- **[P1] "서버 추방하기"가 확인 절차 없이 즉시 실행됨** — 실수로 다른 서버를 잘못 눌러 추방할 위험.
- **[P1] "나가기"도 확인 없이 즉시 실행됨** — 혼자 나가는 게 아니라 같이 대기 중인 다른 서버들에게도 영향이 감.
- **[P2] 준비 상태가 채팅 메시지로만 흘러가고, 화면(임베드)에 "누가 준비했는지" 목록이 없음** — 메시지가 스크롤되면 누가 준비했는지 알 방법이 없음.
- **[P2] 한 번 준비를 누르면 다시 "준비 취소"할 방법이 없음.**
- **[P2] "시작" 버튼을 눌러야만 부족한 조건(태그 미선택/문제 0개/서버 2개 미만)을 하나씩 알려줌** — 미리 체크리스트로 몇 개 조건이 남았는지 보여주면 시행착오가 줄어듦.
- **[P3] 모달의 문제 수 라벨이 "최대 50"으로 돼 있지만 실제 멀티플레이 상한은 60** — 첫 화면부터 잘못된 숫자를 안내.
- **[P3] 로비 안에서 순위표(스코어보드)를 볼 방법이 없음** — 로비 목록 화면에만 순위표 버튼이 있고, 로비에 들어오면 나갔다가 다시 봐야 함.
- **[P3] "어라... 이럴리가 없는뎅..." 같은 장난스러운 말투 에러 메시지**가 다른 화면의 격식 있는 문체와 안 어울림.

### B-7. 스코어보드 (`scoreboard-ui.js`)

- **[P2] 새로고침 버튼이 없음** — 최신 순위를 보려면 화면을 닫았다가 다시 열어야 함(로비 목록 화면엔 있는데 여기만 없음).
- **[P2] 자기 서버가 TOP 10 밖이면 몇 등인지 전혀 알 수 없음** — 승/패/MMR만 보이고 순위는 안 보임.
- **[P3] "[베타 시즌]"이 코드에 하드코딩**돼 있어 시즌이 바뀌어도 배포 없이는 못 바꿈. `scoreboard_ui` 관련 문구가 `text_contents.json`에 아예 없음(다른 화면과 다른 컨벤션).

### B-8. 공지사항 (`note-select-ui.js`, `note-ui.js`)

- **[P2] 화면 제목이 "공지 / 패치노트"인데 패치노트 카테고리 버튼은 이미 삭제되고 없음** — 제목이 실제 기능보다 과장돼 있음.
- **[P2] 목록에 파일명만 나열되고 날짜/한 줄 요약이 없음** — 언제 올라온 공지인지 보려면 일일이 열어봐야 함(파일의 수정시각 정보는 이미 갖고 있는데 목록에 안 씀).
- **[P3] 공지 파일을 열 때 `fs.readFileSync`에 try/catch가 없어서, 목록 조회 후 파일이 삭제/이동되면 에러가 그대로 터짐.**
- **[P3] 정렬 기준이 파일명 가나다순(주석 처리된 mtime 정렬 코드가 있음)이라 실제 게시 순서와 다를 수 있음.**
- **[P3] 공지/공식퀴즈 화면 footer에 옛 디스코드 태그(`제육보끔#1916`)가 하드코딩** — 유저네임 체계 변경 이전 표기가 남아있음.

### B-9. 퀴즈 유형 선택 / 공식 퀴즈 브라우저 (`select-quiz-type-ui.js`, `dev-quiz-select-ui.js`)

- **[P2] 공식 퀴즈 폴더를 깊이 들어갈수록 지금 몇 단계인지 알 방법이 없음(브레드크럼 없음)** — 최상위로 돌아가려면 "뒤로가기"를 폴더 깊이만큼 반복해서 눌러야 함.
- **[P3] 폴더 목록이 봇 시작 시 한 번만 캐싱**돼서, 운영 중 파일을 추가/삭제해도 재시작 전까진 반영 안 되고, 새로고침 버튼도 없음.

### B-10. 관리자 패널 (`admin-panel-ui.js`, `admin-ban-list-ui.js`)

- **[P2] 밴 해제가 확인 절차 없이 즉시 실행됨** — 목록에 ID만 있고(유저/서버 이름, 밴 사유, 날짜 없음) 어떤 걸 골랐는지 다시 확인할 방법 없이 바로 해제됨.
- **[P2] 밴이 25개를 넘으면 select 메뉴에 다 안 보이는데, 안내 문구는 여전히 전체 개수를 보여줌** — 실제로 선택 가능한 것과 화면에 적힌 총 개수가 어긋남(이미 알려진 Discord 25개 제한 — 문서에도 기록돼 있었지만 안내 문구까지는 손 안 댔었음).
- **[P3] "밴 목록 관리"만 빨간(Danger) 버튼, 나머지 둘은 파란(Primary) 버튼** — 정작 밴 목록 화면 자체는 조회+해제만 하는 화면이라 색이 위험도를 정확히 반영하진 않음.
- **[P3] 이 화면에서 새로 밴을 추가할 방법이 없음** — 추가는 여전히 퀴즈 삭제 화면 경유로만 가능, 밴 목록 관리 화면은 조회/해제 전용.

---

## 권장 실행 순서

1. **A번(실제 버그) 1~3번** — 각각 한 함수짜리 국소 수정, 리스크 낮음. 먼저 처리 권장.
2. **B-4의 옵션 저장 토스트 raw 값 노출** — 사용자가 거의 매번 보는 화면이라 체감 임팩트가 큼, 수정 범위도 한 함수(`handleOptionValueSelected`)로 국한됨.
3. **B-5/B-6의 "확인 없는 파괴적 동작"(서버 추방, 로비 나가기) + 채널 전체 공개 에러 메시지 ephemeral로 전환** — 1라운드에서 이미 다룬 "파괴적 동작 안전장치" 패턴과 결이 같아서 같이 묶어 처리하기 좋음.
4. **B-2(신고 플로우) 문구 다듬기** — "No Cache Content" 노출 제거, 모달 제목 변경, 사유 안내 추가. 텍스트 위주라 리스크 낮음.
5. **A번 5~7(죽은 버튼/코드 정리)** — Phase 6 스타일의 정리 작업으로 묶어서 처리 가능.
6. **나머지 [P2]/[P3]**(새로고침 버튼 추가, 브레드크럼, 순위 표시, 준비 취소, 도움말/온보딩, unhandledRejection 전역 처리 등)는 각각 화면/기능 단위가 크거나 설계가 더 필요한 것들이라 우선순위 정해서 순서대로 진행하는 걸 권장. 특히 "온보딩/도움말 신설"과 "unhandledRejection 전역 처리"는 기존 화면 수정이 아니라 새 기능/아키텍처 추가라 별도 계획으로 분리하는 게 안전.

우선순위 골라주시면 그 항목부터 바로 진행하겠습니다.
